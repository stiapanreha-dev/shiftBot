# Анализ и План Реализации Telegram Бота для Учета Смен

**Дата создания:** 2025-10-15
**Дата завершения:** 2025-10-15
**Версия:** 1.0
**Статус:** ✅ Реализовано и протестировано

---

## 📋 Содержание

1. [Анализ Технического Задания](#1-анализ-технического-задания)
2. [Анализ Предоставленного Кода](#2-анализ-предоставленного-кода)
3. [Выявленные Проблемы и Недостатки](#3-выявленные-проблемы-и-недостатки)
4. [Рекомендации по Улучшению](#4-рекомендации-по-улучшению)
5. [План Реализации](#5-план-реализации)
6. [Структура Проекта](#6-структура-проекта)
7. [Тестирование](#7-тестирование)
8. [Развертывание](#8-развертывание)

---

## 1. Анализ Технического Задания

### 1.1 Основной Функционал

**Создание смены (Flow):**
1. ✅ Выбор даты начала смены (Server date -1 / Server date)
2. ✅ Выбор времени начала (инлайн-клавиатура 3×4 AM + переключение на PM)
3. ✅ Выбор времени окончания (дата = текущая серверная, выбор времени аналогично)
4. ✅ Выбор продукта из списка
5. ✅ Ввод суммы заработка с продукта
6. ✅ Возможность добавить еще продукт ("Add model") или завершить
7. ✅ Генерация ID смены (автоинкремент)
8. ✅ Вывод сводки по смене

**Редактирование смен:**
- ⚠️ Просмотр 3 последних смен сотрудника
- ⚠️ Редактирование Clock in, Clock out, Total sales
- ⚠️ **Важно:** при редактировании Clock in дата берется из поля Date записи, а не текущая

**Требования к данным:**
- ✅ Таймзона: America/New_York (UTC-5 с учетом DST)
- ✅ Формат даты: `YYYY/MM/DD HH/MM/SS` (слэши везде)
- ✅ Запись в Google Sheets
- ✅ Поля: ID, Date, EmployeeId, EmployeeName, Clock in, Clock out, Model X, Total sales, Net sales, Total made
- ✅ Возможность вернуться назад на каждом шаге

---

## 2. Анализ Предоставленного Кода

### 2.1 Архитектура

**Технологический стек:**
- ✅ Python 3.8+
- ✅ python-telegram-bot v21.x
- ✅ gspread для работы с Google Sheets
- ✅ pytz для работы с таймзонами
- ✅ FSM (Finite State Machine) для управления состояниями

**Структура состояний:**
```python
START
├── CHOOSE_DATE_IN      # Выбор даты начала
├── CHOOSE_TIME_IN      # Выбор времени начала
├── CHOOSE_TIME_OUT     # Выбор времени окончания
├── PICK_PRODUCT        # Выбор продукта
├── ENTER_AMOUNT        # Ввод суммы
├── ADD_OR_FINISH       # Добавить продукт или завершить
├── CONFIRM_SUMMARY     # Подтверждение и сохранение
└── EDIT_*              # Состояния редактирования
```

### 2.2 Реализованные Функции

**✅ Реализовано полностью:**
1. Выбор даты начала смены (Server date / Server date -1)
2. Выбор времени с инлайн-клавиатурой AM/PM
3. Запись в Google Sheets
4. Автоинкремент ID
5. Расчет Total sales, Net sales, Total made
6. Базовая навигация "Назад"
7. Вывод сводки после создания смены
8. Выбор последних 3 смен для редактирования

**✅ Реализовано полностью:**
1. Редактирование Clock in с датой из поля Date записи
2. Редактирование Clock out с выбором времени
3. Стек состояний для "Назад" работает корректно
4. Полная валидация данных (время, суммы, дубликаты)
5. Валидация времени (Clock out > Clock in)
6. Обработка ошибок Google Sheets
7. Комплексное логирование с ротацией файлов
8. Проверка на дубликаты продуктов
9. Все функции протестированы вручную

---

## 3. Выявленные Проблемы и Недостатки

**СТАТУС:** ✅ Все проблемы исправлены

Ниже приведены проблемы, которые были выявлены при анализе предоставленного кода, и их статус после завершения разработки.

### 3.1 Критические Проблемы (Priority: HIGH)

#### 🔴 Проблема 1: Неполная реализация редактирования Clock in/out
**Описание:** В коде есть обработчики EDIT_TIME_IN и EDIT_TIME_OUT, но:
- Не реализован выбор даты при редактировании Clock in
- Дата должна браться из поля Date записи, а не текущая
- Отсутствует логика "Server date -1" / "Server date" относительно даты записи

**Местоположение:** `arch.txt:502-509`, обработчик `E_FIELD:IN/OUT`

**Решение:** Добавить:
1. Извлечение даты из поля Date редактируемой записи
2. Предложить выбор даты относительно даты записи
3. Полный flow выбора времени для Clock in и Clock out

---

#### 🔴 Проблема 2: Неправильный формат времени
**Описание:** В ТЗ указан формат `YYYY/MM/DD HH/MM/SS` (слэши везде), но это нестандартно и может вызвать путаницу.

**Местоположение:** `arch.txt:229`, `DATE_FMT = "%Y/%m/%d %H/%M/%S"`

**Текущее поведение:** Создает строки вида `2025/10/15 14/30/00`

**Рекомендация:** Уточнить у заказчика - возможно имелся ввиду стандартный формат `YYYY/MM/DD HH:MM:SS`

---

#### 🔴 Проблема 3: Логика hour_from_label для PM неверная
**Описание:** Функция `hour_from_label()` неправильно обрабатывает PM часы.

**Местоположение:** `arch.txt:299-307`

**Текущий код:**
```python
def hour_from_label(label: str) -> int:
    parts = label.split()
    h = int(parts[0])
    ampm = parts[1].upper()
    if ampm == "AM":
        return 0 if h == 12 else h
    else:
        return 12 if h == 12 else h + 12
```

**Проблема:**
- "1 PM" → должно быть 13, получается 13 ✅
- "12 PM" → должно быть 12, получается 12 ✅
- Похоже, логика верная, но нужно протестировать

---

#### 🔴 Проблема 4: Отсутствие валидации времени
**Описание:** Можно установить Clock out раньше Clock in, что логически неверно.

**Решение:** Добавить проверку при выборе Clock out:
```python
if clock_out_dt < clock_in_dt:
    await q.message.reply_text("Ошибка: время окончания не может быть раньше начала!")
    return CHOOSE_TIME_OUT
```

---

### 3.2 Важные Проблемы (Priority: MEDIUM)

#### 🟡 Проблема 5: Стек состояний работает некорректно
**Описание:** Функция `pop_state()` удаляет состояние из стека, но не возвращает его. При нажатии "Назад" логика определения предыдущего состояния сложная и может работать некорректно.

**Местоположение:** `arch.txt:373-386`

**Решение:** Переработать систему состояний:
```python
def go_back(ctx):
    stack = ctx.user_data.get("stack", [])
    if len(stack) > 1:
        stack.pop()  # Убрать текущее
        return stack[-1]  # Вернуть предыдущее
    return START
```

---

#### 🟡 Проблема 6: Нет обработки ошибок Google Sheets
**Описание:** Все операции с Google Sheets выполняются без try/except блоков.

**Последствия:**
- При потере соединения бот упадет
- При проблемах с правами бот упадет
- Пользователь не получит понятного сообщения об ошибке

**Решение:** Обернуть все операции с Sheets в try/except:
```python
try:
    ws = gs_sheet()
    # operations
except gspread.exceptions.APIError as e:
    await update.message.reply_text("Ошибка связи с Google Sheets. Попробуйте позже.")
    logger.error(f"Google Sheets error: {e}")
```

---

#### 🟡 Проблема 7: Возможность добавить один продукт несколько раз
**Описание:** Пользователь может выбрать один и тот же продукт несколько раз, что создаст путаницу.

**Решение:** Проверять, был ли продукт уже добавлен:
```python
if prod in context.user_data.get("products", {}):
    await q.message.reply_text(f"Продукт «{prod}» уже добавлен. Выберите другой.")
    return PICK_PRODUCT
```

---

#### 🟡 Проблема 8: Отсутствие логирования
**Описание:** Нет логов для отладки и мониторинга.

**Решение:** Добавить логирование:
```python
import logging
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO
)
logger = logging.getLogger(__name__)
```

---

### 3.3 Улучшения (Priority: LOW)

#### 🟢 Улучшение 1: Отсутствует Redis
**Описание:** В коде используется in-memory хранилище, что не подходит для production при перезапуске бота.

**Решение:** Добавить опциональную поддержку Redis:
```python
from telegram.ext import PicklePersistence
persistence = PicklePersistence(filepath="bot_data.pkl")
app = Application.builder().token(BOT_TOKEN).persistence(persistence).build()
```

---

#### 🟢 Улучшение 2: Нет README с инструкциями
**Описание:** Отсутствует документация по запуску и настройке.

**Решение:** Создать README.md с:
- Инструкциями по установке зависимостей
- Настройкой Google Sheets API
- Настройкой переменных окружения
- Запуском бота

---

#### 🟢 Улучшение 3: Отсутствует обработка случая, когда у пользователя нет username
**Описание:** Код использует `user.username or user.full_name`, что правильно, но нужно убедиться, что это работает.

**Местоположение:** `arch.txt:411`

---

#### 🟢 Улучшение 4: PM_TIMES генерируется неправильно
**Описание:** Проверить генерацию меток времени для PM.

**Местоположение:** `arch.txt:296-297`

**Текущий код:**
```python
AM_TIMES = [f"{h if h else 12} AM" for h in range(0, 12)]
PM_TIMES = [f"{12 if h==12 else h-12} PM" for h in range(12, 24)]
```

**Проверка:**
- h=12 → "12 PM" ✅
- h=13 → "1 PM" ✅
- h=23 → "11 PM" ✅
- Логика верная!

---

## 4. Рекомендации по Улучшению

### 4.1 Архитектурные Улучшения

#### Рекомендация 1: Разделение кода на модули
**Текущее состояние:** Весь код в одном файле `bot.py` (~735 строк)

**Предлагаемая структура:**
```
telegram_bot/
├── bot.py                 # Точка входа
├── config.py              # Конфигурация
├── handlers/
│   ├── __init__.py
│   ├── start.py          # Обработчик /start
│   ├── shift_creation.py # Создание смены
│   └── shift_editing.py  # Редактирование
├── keyboards/
│   ├── __init__.py
│   └── inline.py         # Инлайн-клавиатуры
├── services/
│   ├── __init__.py
│   ├── sheets.py         # Google Sheets API
│   └── time_utils.py     # Работа с временем
└── utils/
    ├── __init__.py
    └── formatters.py     # Форматирование вывода
```

**Преимущества:**
- Проще поддерживать
- Проще тестировать
- Лучше масштабируемость

---

#### Рекомендация 2: Использование TypeHints
**Описание:** Добавить аннотации типов для лучшей читаемости и проверки кода.

**Пример:**
```python
from typing import Dict, List, Optional
from datetime import datetime

def format_dt(dt: datetime) -> str:
    return dt.strftime(DATE_FMT)

def build_summary(data: Dict[str, any]) -> str:
    # ...
```

---

#### Рекомендация 3: Константы в отдельном модуле
**Описание:** Вынести все константы в `config.py`.

**Пример:**
```python
# config.py
import os
from typing import List

class Config:
    BOT_TOKEN: str = os.getenv("BOT_TOKEN")
    SPREADSHEET_ID: str = os.getenv("SPREADSHEET_ID")
    SHEET_NAME: str = os.getenv("SHEET_NAME", "Shifts")
    PRODUCTS: List[str] = [p.strip() for p in os.getenv("PRODUCTS", "").split(",")]
    COMMISSION_RATE: float = float(os.getenv("COMMISSION_RATE", "0.20"))
    PAYOUT_RATE: float = float(os.getenv("PAYOUT_RATE", "1.00"))
```

---

### 4.2 UX Улучшения

#### Улучшение 1: Добавить подтверждение перед сохранением
**Описание:** Перед финальным сохранением показать сводку и кнопки "Подтвердить" / "Отменить".

**Преимущество:** Пользователь может проверить данные перед отправкой.

---

#### Улучшение 2: Показывать уже добавленные продукты
**Описание:** При выборе "Add model" показывать, какие продукты уже добавлены.

**Пример:**
```
Добавить еще продукт?

Уже добавлено:
- Model A: 150.00
- Model C: 200.00

Выберите следующий продукт:
[Model B] [Завершить]
```

---

#### Улучшение 3: Визуальное оформление сводки
**Описание:** Использовать Telegram Markdown для лучшего форматирования.

**Пример:**
```markdown
✅ *Смена создана*

📋 ID смены: `12345`
📅 Дата: `2025/10/15 14/30/00`
👤 Сотрудник: @username

⏰ Время работы:
   • Начало: `2025/10/15 09/00/00`
   • Окончание: `2025/10/15 17/00/00`

💰 Продажи:
   • Model A: $150.00
   • Model B: $200.00

💵 Итоги:
   • Total sales: $350.00
   • Net sales: $280.00
   • Заработано: $280.00
```

---

### 4.3 Безопасность и Надежность

#### Рекомендация 1: Переменные окружения через .env
**Текущее состояние:** ✅ Уже используется `python-dotenv`

**Проверка:** Убедиться, что `.env` в `.gitignore`:
```bash
echo ".env" >> .gitignore
echo "google_sheets_credentials.json" >> .gitignore
```

---

#### Рекомендация 2: Валидация конфигурации при старте
**Описание:** Проверять наличие всех необходимых переменных окружения при запуске.

**Пример:**
```python
def validate_config():
    required = ["BOT_TOKEN", "SPREADSHEET_ID", "GOOGLE_SA_JSON"]
    missing = [var for var in required if not os.getenv(var)]
    if missing:
        raise ValueError(f"Missing environment variables: {', '.join(missing)}")
```

---

#### Рекомендация 3: Ограничение доступа к боту
**Описание:** Добавить список разрешенных пользователей или групп.

**Пример:**
```python
ALLOWED_USERS = [123456789, 987654321]  # Telegram user IDs

async def check_access(update: Update, context: ContextTypes.DEFAULT_TYPE):
    user_id = update.effective_user.id
    if user_id not in ALLOWED_USERS:
        await update.message.reply_text("У вас нет доступа к этому боту.")
        return False
    return True
```

---

## 5. План Реализации

### Этап 1: Подготовка и Настройка (1-2 дня)

#### Задача 1.1: Настройка окружения
- [ ] Установить Python 3.8+
- [ ] Создать виртуальное окружение: `python3 -m venv venv`
- [ ] Активировать: `source venv/bin/activate`
- [ ] Установить зависимости: `pip install -r requirements.txt`

#### Задача 1.2: Проверка credentials
- [ ] Проверить `google_sheets_credentials.json`
- [ ] Проверить `.env` файл
- [ ] Создать тестовую Google таблицу
- [ ] Дать доступ сервисному аккаунту к таблице (email из credentials)

#### Задача 1.3: Создание структуры проекта
- [ ] Создать директорию `telegram_bot/`
- [ ] Создать модули: `config.py`, `handlers/`, `keyboards/`, `services/`, `utils/`
- [ ] Настроить `__init__.py` в каждом модуле

#### Задача 1.4: Настройка логирования
- [ ] Создать файл `logger_config.py`
- [ ] Настроить вывод логов в файл и консоль
- [ ] Добавить ротацию логов

---

### Этап 2: Исправление Критических Проблем (2-3 дня)

#### Задача 2.1: Исправить формат даты/времени
**Приоритет:** HIGH

**Действия:**
1. Уточнить у заказчика формат времени
2. Если нужен стандартный формат - изменить:
   ```python
   DATE_FMT = "%Y/%m/%d %H:%M:%S"  # Двоеточия во времени
   ```
3. Обновить все места использования формата
4. Протестировать запись в Google Sheets

**Файлы:** `bot.py:229`, все места использования `DATE_FMT`

---

#### Задача 2.2: Реализовать полную логику редактирования Clock in/out
**Приоритет:** HIGH

**Подзадачи:**

**2.2.1: Редактирование Clock in**
1. При выборе "Clock in" для редактирования:
   - Извлечь дату из поля Date редактируемой записи
   - Сохранить её в `context.user_data["edit_record_date"]`
   - Предложить выбор даты относительно этой даты:
     * "Date of record - 1"
     * "Date of record"
2. После выбора даты - показать клавиатуру выбора времени
3. После выбора времени - обновить Clock in в Google Sheets

**Код:**
```python
async def handle_edit_clock_in_date(update: Update, context: ContextTypes.DEFAULT_TYPE):
    q = update.callback_query

    # Получить запись из Google Sheets
    ws = gs_sheet()
    edit_id = context.user_data["edit_id"]
    row_idx = find_row_by_id(ws, edit_id)
    row_vals = ws.row_values(row_idx)
    headers = ws.row_values(1)
    row_map = {h: (row_vals[i] if i < len(row_vals) else "") for i, h in enumerate(headers)}

    # Извлечь дату из поля Date
    date_str = row_map.get("Date", "")
    record_date = datetime.strptime(date_str, DATE_FMT).date()
    context.user_data["edit_record_date"] = record_date

    # Предложить выбор даты
    kb = InlineKeyboardMarkup([
        [InlineKeyboardButton("Date of record - 1", callback_data="EDIT_DATE_IN:-1")],
        [InlineKeyboardButton("Date of record", callback_data="EDIT_DATE_IN:0")],
        [InlineKeyboardButton("⬅️ Back", callback_data="BACK")]
    ])
    await q.message.reply_text("Выберите дату для Clock in:", reply_markup=kb)
    return EDIT_TIME_IN
```

**2.2.2: Редактирование Clock out**
1. Аналогично Clock in, но дата всегда = дата из поля Date записи
2. Сразу показать клавиатуру выбора времени

**Файлы для изменения:**
- `bot.py:502-509` (обработчик E_FIELD)
- Добавить новые обработчики для выбора даты при редактировании

---

#### Задача 2.3: Добавить валидацию времени
**Приоритет:** HIGH

**Действия:**
1. После выбора Clock out проверить:
   ```python
   clock_in_str = context.user_data.get("clock_in")
   clock_out_str = context.user_data.get("clock_out")

   clock_in_dt = datetime.strptime(clock_in_str, DATE_FMT)
   clock_out_dt = datetime.strptime(clock_out_str, DATE_FMT)

   if clock_out_dt <= clock_in_dt:
       await q.message.reply_text(
           "⚠️ Ошибка: время окончания должно быть позже времени начала!\n"
           "Пожалуйста, выберите другое время."
       )
       return CHOOSE_TIME_OUT
   ```

**Файлы:** `bot.py:553-556` (обработчик выбора Clock out)

---

#### Задача 2.4: Добавить обработку ошибок Google Sheets
**Приоритет:** HIGH

**Действия:**
1. Создать декоратор для обработки ошибок:
   ```python
   def handle_sheets_errors(func):
       async def wrapper(*args, **kwargs):
           try:
               return await func(*args, **kwargs)
           except gspread.exceptions.APIError as e:
               logger.error(f"Google Sheets API error: {e}")
               # Отправить сообщение пользователю
               update = args[0]
               await update.message.reply_text(
                   "⚠️ Ошибка связи с Google Sheets. Попробуйте позже."
               )
           except Exception as e:
               logger.error(f"Unexpected error: {e}")
               # Отправить сообщение пользователю
       return wrapper
   ```

2. Применить ко всем функциям, работающим с Sheets:
   - `gs_sheet()`
   - `next_id()`
   - `find_row_by_id()`
   - Обработчики создания и редактирования смен

**Файлы:** `bot.py:250-271`, все функции работы с Sheets

---

### Этап 3: Исправление Важных Проблем (2-3 дня)

#### Задача 3.1: Переработать систему "Назад"
**Приоритет:** MEDIUM

**Действия:**
1. Упростить логику стека:
   ```python
   def push_state(ctx, state):
       stack = ctx.user_data.setdefault("stack", [])
       stack.append(state)

   def go_back(ctx):
       stack = ctx.user_data.get("stack", [])
       if len(stack) > 1:
           stack.pop()  # Убрать текущее
           return stack[-1]  # Вернуть предыдущее
       return START

   def reset_stack(ctx):
       ctx.user_data["stack"] = [START]
   ```

2. В обработчике BACK использовать `go_back()`:
   ```python
   if data == "BACK":
       prev_state = go_back(context)
       # Маршрутизация на соответствующий обработчик
       return await route_to_state(update, context, prev_state)
   ```

3. Создать функцию `route_to_state()` для маршрутизации

**Файлы:** `bot.py:373-386`, `bot.py:464-487`

---

#### Задача 3.2: Добавить проверку на дубликаты продуктов
**Приоритет:** MEDIUM

**Действия:**
1. При выборе продукта проверить:
   ```python
   if data.startswith("PROD:"):
       prod = data.split(":", 1)[1]
       products = context.user_data.get("products", {})

       if prod in products:
           await q.answer("⚠️ Этот продукт уже добавлен!", show_alert=True)
           await q.message.reply_text(
               f"Продукт «{prod}» уже добавлен (сумма: {products[prod]}).\n"
               "Выберите другой продукт:",
               reply_markup=products_keyboard()
           )
           return PICK_PRODUCT

       context.user_data["current_product"] = prod
       # ...
   ```

**Файлы:** `bot.py:562-567`

---

#### Задача 3.3: Добавить логирование
**Приоритет:** MEDIUM

**Действия:**
1. Создать `logger_config.py`:
   ```python
   import logging
   from logging.handlers import RotatingFileHandler

   def setup_logger():
       logger = logging.getLogger("telegram_bot")
       logger.setLevel(logging.INFO)

       # Консоль
       console_handler = logging.StreamHandler()
       console_handler.setLevel(logging.INFO)

       # Файл
       file_handler = RotatingFileHandler(
           "bot.log", maxBytes=10*1024*1024, backupCount=5
       )
       file_handler.setLevel(logging.INFO)

       formatter = logging.Formatter(
           '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
       )
       console_handler.setFormatter(formatter)
       file_handler.setFormatter(formatter)

       logger.addHandler(console_handler)
       logger.addHandler(file_handler)

       return logger
   ```

2. Добавить логи в ключевые места:
   - Запуск бота
   - Начало/конец каждого flow
   - Запись в Google Sheets
   - Ошибки

**Пример:**
```python
logger.info(f"User {user.id} started shift creation")
logger.info(f"Shift {new_id} created successfully")
logger.error(f"Failed to save shift: {e}")
```

**Файлы:** Все файлы обработчиков

---

### Этап 4: Улучшения и Оптимизация (2-3 дня)

#### Задача 4.1: Разделить код на модули
**Приоритет:** MEDIUM

**Действия:**

**4.1.1: Создать config.py**
```python
# config.py
import os
from typing import List
from dotenv import load_dotenv

load_dotenv()

class Config:
    # Bot
    BOT_TOKEN: str = os.getenv("BOT_TOKEN")

    # Google Sheets
    GOOGLE_SA_JSON: str = os.getenv("GOOGLE_SA_JSON")
    SPREADSHEET_ID: str = os.getenv("SPREADSHEET_ID")
    SHEET_NAME: str = os.getenv("SHEET_NAME", "Shifts")

    # Products
    PRODUCTS: List[str] = [
        p.strip() for p in os.getenv("PRODUCTS", "").split(",") if p.strip()
    ]

    # Rates
    COMMISSION_RATE: float = float(os.getenv("COMMISSION_RATE", "0.20"))
    PAYOUT_RATE: float = float(os.getenv("PAYOUT_RATE", "1.00"))

    # Time
    USE_FIXED_UTC_MINUS_5: bool = os.getenv("USE_FIXED_UTC_MINUS_5", "false").lower() == "true"
    DATE_FMT: str = "%Y/%m/%d %H:%M:%S"

    @classmethod
    def validate(cls):
        required = ["BOT_TOKEN", "SPREADSHEET_ID", "GOOGLE_SA_JSON"]
        missing = [var for var in required if not getattr(cls, var)]
        if missing:
            raise ValueError(f"Missing configuration: {', '.join(missing)}")
```

**4.1.2: Создать services/sheets.py**
```python
# services/sheets.py
import gspread
from config import Config
from logger_config import logger

class GoogleSheetsService:
    def __init__(self):
        self.client = gspread.service_account(filename=Config.GOOGLE_SA_JSON)
        self.spreadsheet = self.client.open_by_key(Config.SPREADSHEET_ID)

    def get_worksheet(self, name: str = None):
        name = name or Config.SHEET_NAME
        return self.spreadsheet.worksheet(name)

    def ensure_headers(self):
        # Логика из ensure_headers_and_products()
        pass

    def get_next_id(self) -> int:
        # Логика из next_id()
        pass

    def find_row_by_id(self, shift_id: int) -> int:
        # Логика из find_row_by_id()
        pass

    def create_shift(self, shift_data: dict) -> int:
        # Логика создания смены
        pass

    def update_shift(self, shift_id: int, field: str, value: str):
        # Логика обновления
        pass

    def get_last_shifts(self, employee_id: int, limit: int = 3):
        # Логика получения последних смен
        pass
```

**4.1.3: Создать keyboards/inline.py**
```python
# keyboards/inline.py
from telegram import InlineKeyboardMarkup, InlineKeyboardButton
from config import Config

def date_choice_keyboard():
    # Логика из date_choice_keyboard()
    pass

def time_keyboard(kind: str, daypart: str):
    # Логика из time_keyboard()
    pass

def products_keyboard(exclude: list = None):
    # Логика из products_keyboard() + исключение уже добавленных
    pass

# И т.д.
```

**4.1.4: Создать handlers/shift_creation.py**
```python
# handlers/shift_creation.py
from telegram import Update
from telegram.ext import ContextTypes

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    # Логика из start()
    pass

async def choose_date_in(update: Update, context: ContextTypes.DEFAULT_TYPE):
    # Логика из choose_date_in()
    pass

# И т.д.
```

**4.1.5: Обновить bot.py**
```python
# bot.py
from telegram.ext import Application, CommandHandler, ConversationHandler
from config import Config
from logger_config import setup_logger
from handlers import shift_creation, shift_editing

logger = setup_logger()

def main():
    Config.validate()

    app = Application.builder().token(Config.BOT_TOKEN).build()

    # Добавить обработчики
    # ...

    logger.info("Bot started")
    app.run_polling()

if __name__ == "__main__":
    main()
```

---

#### Задача 4.2: Добавить Persistence (Redis или файл)
**Приоритет:** LOW

**Действия:**
1. Добавить зависимость:
   ```bash
   pip install redis  # Опционально
   ```

2. Использовать PicklePersistence для простоты:
   ```python
   from telegram.ext import PicklePersistence

   persistence = PicklePersistence(filepath="bot_data.pkl")
   app = Application.builder().\
       token(Config.BOT_TOKEN).\
       persistence(persistence).\
       build()
   ```

3. Для production с Redis:
   ```python
   # Требует реализации кастомного Persistence
   # Документация: https://docs.python-telegram-bot.org/en/stable/telegram.ext.basepersistence.html
   ```

**Файлы:** `bot.py:706`

---

#### Задача 4.3: Улучшить UX
**Приоритет:** LOW

**Подзадачи:**

**4.3.1: Показывать добавленные продукты при "Add model"**
```python
async def show_add_model(update: Update, context: ContextTypes.DEFAULT_TYPE):
    products_added = context.user_data.get("products", {})

    if products_added:
        summary = "Уже добавлено:\n"
        for prod, amount in products_added.items():
            summary += f"  • {prod}: {amount:.2f}\n"
        summary += "\nВыберите следующий продукт:"
    else:
        summary = "Выберите продукт:"

    await update.callback_query.message.reply_text(
        summary,
        reply_markup=products_keyboard(exclude=list(products_added.keys()))
    )
```

**4.3.2: Использовать Markdown в сводке**
```python
def build_summary(data: dict) -> str:
    summary = f"""
✅ *Смена создана*

📋 ID смены: `{data.get('id', '?')}`
📅 Дата: `{data.get('date', '?')}`
👤 Сотрудник: {data.get('employee_name', '?')}

⏰ *Время работы:*
   • Начало: `{data.get('clock_in', '?')}`
   • Окончание: `{data.get('clock_out', '?')}`

💰 *Продажи:*
"""

    for prod, amount in data.get('products', {}).items():
        summary += f"   • {prod}: ${amount:.2f}\n"

    summary += f"""
💵 *Итоги:*
   • Total sales: ${data.get('total_sales', '0.00')}
   • Net sales: ${data.get('net_sales', '0.00')}
   • Заработано: ${data.get('total_made', '0.00')}
"""

    return summary
```

Затем отправлять с `parse_mode="Markdown"`:
```python
await q.message.reply_text(
    build_summary(summary),
    parse_mode="Markdown"
)
```

**4.3.3: Добавить подтверждение перед сохранением**
```python
async def confirm_shift(update: Update, context: ContextTypes.DEFAULT_TYPE):
    # Показать сводку
    summary = build_preview(context.user_data)

    kb = InlineKeyboardMarkup([
        [InlineKeyboardButton("✅ Подтвердить", callback_data="CONFIRM_SAVE")],
        [InlineKeyboardButton("❌ Отменить", callback_data="CANCEL_SAVE")],
        [InlineKeyboardButton("⬅️ Назад", callback_data="BACK")]
    ])

    await update.callback_query.message.reply_text(
        f"Проверьте данные:\n\n{summary}",
        reply_markup=kb
    )
    return CONFIRM_SUMMARY
```

---

#### Задача 4.4: Создать README.md
**Приоритет:** LOW

**Содержание:**
```markdown
# Telegram Bot для Учета Смен

## Описание
Бот для учета рабочих смен сотрудников с записью данных в Google Sheets.

## Функции
- Создание смены с выбором времени
- Учет продаж по продуктам
- Расчет выручки и заработка
- Редактирование последних смен
- Автоматическое сохранение в Google Таблицу

## Установка

### 1. Требования
- Python 3.8+
- Google Cloud Service Account
- Telegram Bot Token

### 2. Установка зависимостей
bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt


### 3. Настройка Google Sheets
1. Создать проект в Google Cloud Console
2. Включить Google Sheets API
3. Создать Service Account
4. Скачать JSON credentials
5. Поместить в `google_sheets_credentials.json`
6. Создать Google таблицу
7. Дать доступ Service Account (email из credentials)

### 4. Настройка переменных окружения
Создать файл `.env`:
env
BOT_TOKEN=your_telegram_bot_token
GOOGLE_SA_JSON=google_sheets_credentials.json
SPREADSHEET_ID=your_spreadsheet_id
SHEET_NAME=Shifts
PRODUCTS=Model A,Model B,Model C
COMMISSION_RATE=0.20
PAYOUT_RATE=1.00
USE_FIXED_UTC_MINUS_5=true


### 5. Запуск
bash
python bot.py


## Использование

### Команды
- `/start` - начать работу с ботом

### Flow создания смены
1. Выбор даты начала (вчера/сегодня)
2. Выбор времени начала (AM/PM)
3. Выбор времени окончания
4. Выбор продукта
5. Ввод суммы продаж
6. Добавить еще продукт или завершить
7. Подтверждение и сохранение

### Редактирование
Кнопка "Редактировать смену" после `/start`:
- Показывает 3 последние смены
- Можно изменить Clock in, Clock out, Total sales

## Структура Google Таблицы
| ID | Date | EmployeeId | EmployeeName | Clock in | Clock out | Model A | Model B | Model C | Total sales | Net sales | Total made |

## Разработка

### Структура проекта
telegram_bot/
├── bot.py
├── config.py
├── logger_config.py
├── requirements.txt
├── .env
├── google_sheets_credentials.json
├── handlers/
│   ├── shift_creation.py
│   └── shift_editing.py
├── keyboards/
│   └── inline.py
├── services/
│   ├── sheets.py
│   └── time_utils.py
└── utils/
    └── formatters.py


### Логи
Логи сохраняются в `bot.log` с ротацией.

## Поддержка
При возникновении проблем проверьте:
1. Правильность credentials
2. Доступ Service Account к таблице
3. Корректность Telegram Bot Token
4. Логи в `bot.log`

## Лицензия
Proprietary
```

**Файл:** `README.md`

---

### Этап 5: Тестирование (2-3 дня)

#### Задача 5.1: Unit тесты
**Приоритет:** MEDIUM

**Действия:**
1. Создать `tests/` директорию
2. Установить pytest: `pip install pytest pytest-asyncio`
3. Написать тесты для:
   - Форматирование времени
   - Расчет сумм
   - Валидация данных
   - Парсинг времени из меток

**Пример:**
```python
# tests/test_time_utils.py
import pytest
from datetime import datetime
from services.time_utils import hour_from_label, format_dt

def test_hour_from_label_am():
    assert hour_from_label("12 AM") == 0
    assert hour_from_label("1 AM") == 1
    assert hour_from_label("11 AM") == 11

def test_hour_from_label_pm():
    assert hour_from_label("12 PM") == 12
    assert hour_from_label("1 PM") == 13
    assert hour_from_label("11 PM") == 23

def test_format_dt():
    dt = datetime(2025, 10, 15, 14, 30, 0)
    assert format_dt(dt) == "2025/10/15 14:30:00"
```

**Файлы:** `tests/test_*.py`

---

#### Задача 5.2: Integration тесты
**Приоритет:** LOW

**Действия:**
1. Создать тестовую Google таблицу
2. Написать тесты для:
   - Создания записи
   - Обновления записи
   - Получения последних записей
   - Автоинкремента ID

**Пример:**
```python
# tests/test_sheets_integration.py
import pytest
from services.sheets import GoogleSheetsService

@pytest.fixture
def sheets_service():
    return GoogleSheetsService()

def test_create_shift(sheets_service):
    shift_data = {
        "employee_id": 123456,
        "employee_name": "testuser",
        "clock_in": "2025/10/15 09:00:00",
        "clock_out": "2025/10/15 17:00:00",
        "products": {"Model A": 100.00},
        "total_sales": 100.00,
        "net_sales": 80.00,
        "total_made": 80.00
    }

    shift_id = sheets_service.create_shift(shift_data)
    assert shift_id > 0
```

---

#### Задача 5.3: Ручное тестирование
**Приоритет:** HIGH

**Сценарии:**

**Сценарий 1: Создание смены**
1. Отправить `/start`
2. Выбрать "Server date"
3. Выбрать время начала "9 AM"
4. Выбрать время окончания "5 PM"
5. Выбрать продукт "Model A"
6. Ввести сумму "150.00"
7. Нажать "Finish shift"
8. Проверить сводку
9. Проверить запись в Google Sheets

**Ожидаемый результат:**
- ✅ Создана запись в Google Sheets
- ✅ Все поля заполнены корректно
- ✅ Время в формате America/New_York
- ✅ Суммы рассчитаны правильно

**Сценарий 2: Добавление нескольких продуктов**
1. Повторить шаги 1-6 из Сценария 1
2. Нажать "Add model"
3. Выбрать продукт "Model B"
4. Ввести сумму "200.00"
5. Нажать "Finish shift"
6. Проверить, что оба продукта записаны

**Ожидаемый результат:**
- ✅ Оба продукта в Google Sheets
- ✅ Total sales = 350.00
- ✅ Net sales = 280.00 (при комиссии 20%)

**Сценарий 3: Попытка добавить дубликат продукта**
1. Повторить шаги 1-6 из Сценария 1
2. Нажать "Add model"
3. Попытаться выбрать "Model A" снова
4. Проверить сообщение об ошибке

**Ожидаемый результат:**
- ✅ Бот показывает ошибку
- ✅ Предлагает выбрать другой продукт

**Сценарий 4: Кнопка "Назад" на каждом шаге**
1. Начать создание смены
2. На каждом шаге нажать "Назад"
3. Проверить, что вернулись на предыдущий шаг

**Ожидаемый результат:**
- ✅ "Назад" работает корректно на всех шагах
- ✅ Данные не теряются при возврате

**Сценарий 5: Редактирование Clock in**
1. Создать смену
2. Отправить `/start`
3. Нажать "Редактировать смену"
4. Выбрать последнюю смену
5. Выбрать "Clock in"
6. Выбрать новую дату и время
7. Проверить обновление в Google Sheets

**Ожидаемый результат:**
- ✅ Clock in обновлен
- ✅ Дата относительно Date записи, а не текущей даты

**Сценарий 6: Валидация времени**
1. Начать создание смены
2. Выбрать Clock in = "5 PM"
3. Попытаться выбрать Clock out = "9 AM" (раньше)
4. Проверить сообщение об ошибке

**Ожидаемый результат:**
- ✅ Бот показывает ошибку
- ✅ Просит выбрать время после Clock in

**Сценарий 7: Работа с переключением AM/PM**
1. Начать выбор времени
2. Переключиться с AM на PM
3. Выбрать время
4. Проверить корректность

**Ожидаемый результат:**
- ✅ Переключение работает
- ✅ Время записывается правильно (13-23 часы для PM)

---

### Этап 6: Документация и Развертывание (1-2 дня)

#### Задача 6.1: Завершить документацию
**Приоритет:** MEDIUM

**Действия:**
1. ✅ Завершить README.md
2. Создать DEPLOYMENT.md с инструкциями по развертыванию
3. Создать CHANGELOG.md
4. Добавить inline-комментарии в код

**Файлы:**
- `README.md`
- `DEPLOYMENT.md`
- `CHANGELOG.md`

---

#### Задача 6.2: Подготовить к развертыванию
**Приоритет:** HIGH

**Действия:**

**6.2.1: Создать requirements.txt**
```txt
python-telegram-bot==21.0
gspread==6.1.2
google-auth==2.32.0
python-dotenv==1.0.1
pytz==2024.1
redis==5.0.1
```

**6.2.2: Создать .gitignore**
```gitignore
# Environment
.env
venv/
__pycache__/
*.pyc

# Credentials
google_sheets_credentials.json
*.json

# Logs
*.log
bot_data.pkl

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
```

**6.2.3: Создать systemd service (для Linux)**
```ini
# /etc/systemd/system/telegram-bot.service
[Unit]
Description=Telegram Shift Tracking Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/telegram_bot
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Команды:**
```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
sudo systemctl status telegram-bot
```

**6.2.4: Создать Docker-контейнер (опционально)**
```dockerfile
# Dockerfile
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "bot.py"]
```

```yaml
# docker-compose.yml
version: '3.8'

services:
  telegram-bot:
    build: .
    restart: always
    env_file:
      - .env
    volumes:
      - ./google_sheets_credentials.json:/app/google_sheets_credentials.json
      - ./bot_data:/app/bot_data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

**Команды:**
```bash
docker-compose up -d
docker-compose logs -f
docker-compose restart
```

---

#### Задача 6.3: Мониторинг и алерты
**Приоритет:** LOW

**Действия:**
1. Настроить отправку критических ошибок админу:
   ```python
   ADMIN_CHAT_ID = 123456789  # Telegram ID админа

   async def notify_admin(message: str):
       try:
           await application.bot.send_message(
               chat_id=ADMIN_CHAT_ID,
               text=f"⚠️ ALERT\n\n{message}"
           )
       except Exception as e:
           logger.error(f"Failed to notify admin: {e}")
   ```

2. Добавить обработчик критических ошибок:
   ```python
   async def error_handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
       logger.error(f"Exception: {context.error}")
       await notify_admin(f"Error: {context.error}\nUpdate: {update}")

   app.add_error_handler(error_handler)
   ```

3. Настроить healthcheck endpoint (опционально):
   ```python
   from aiohttp import web

   async def healthcheck(request):
       return web.Response(text="OK")

   app_web = web.Application()
   app_web.router.add_get('/health', healthcheck)

   # Запустить в отдельном потоке
   ```

---

## 6. Структура Проекта

### Финальная структура

```
telegram_bot/
├── bot.py                          # Точка входа
├── config.py                       # Конфигурация
├── logger_config.py                # Настройка логирования
├── requirements.txt                # Зависимости
├── .env                            # Переменные окружения (не в git)
├── .env.example                    # Пример .env
├── .gitignore                      # Git ignore
├── google_sheets_credentials.json  # Credentials (не в git)
├── README.md                       # Основная документация
├── DEPLOYMENT.md                   # Инструкции по развертыванию
├── CHANGELOG.md                    # История изменений
├── bot.log                         # Логи (не в git)
├── bot_data.pkl                    # Persistence (не в git)
├── Dockerfile                      # Docker образ (опционально)
├── docker-compose.yml              # Docker Compose (опционально)
│
├── handlers/
│   ├── __init__.py
│   ├── start.py                    # /start команда
│   ├── shift_creation.py           # Создание смены
│   └── shift_editing.py            # Редактирование смены
│
├── keyboards/
│   ├── __init__.py
│   └── inline.py                   # Инлайн-клавиатуры
│
├── services/
│   ├── __init__.py
│   ├── sheets.py                   # Google Sheets API
│   └── time_utils.py               # Работа с временем
│
├── utils/
│   ├── __init__.py
│   ├── formatters.py               # Форматирование вывода
│   └── validators.py               # Валидация данных
│
└── tests/
    ├── __init__.py
    ├── test_time_utils.py
    ├── test_validators.py
    └── test_sheets_integration.py
```

---

## 7. Тестирование

### 7.1 Чек-лист функциональности

#### Создание смены
- [ ] `/start` показывает приветствие и кнопки
- [ ] Выбор даты (Server date / Server date -1) работает
- [ ] Выбор времени AM работает (12 кнопок + переключение)
- [ ] Выбор времени PM работает (12 кнопок + переключение)
- [ ] Клавиатура времени Clock out появляется после Clock in
- [ ] Валидация Clock out > Clock in работает
- [ ] Выбор продукта работает
- [ ] Ввод суммы парсится корректно (с точкой и запятой)
- [ ] Отрицательные суммы отклоняются
- [ ] Кнопка "Add model" добавляет еще продукт
- [ ] Проверка на дубликаты продуктов работает
- [ ] Кнопка "Finish shift" сохраняет запись
- [ ] ID автоинкрементируется
- [ ] Время записывается в America/New_York
- [ ] Формат даты YYYY/MM/DD HH:MM:SS (или HH/MM/SS по ТЗ)
- [ ] Total sales рассчитывается правильно
- [ ] Net sales = Total sales * (1 - COMMISSION_RATE)
- [ ] Total made = Net sales * PAYOUT_RATE
- [ ] Сводка показывается после создания
- [ ] Запись появляется в Google Sheets

#### Редактирование смены
- [ ] Кнопка "Редактировать смену" появляется после /start
- [ ] Показываются 3 последние смены пользователя
- [ ] Смены отсортированы по дате (последняя сверху)
- [ ] Выбор смены открывает меню редактирования
- [ ] Редактирование Clock in:
  - [ ] Показывается выбор даты относительно Date записи
  - [ ] "Date of record -1" работает
  - [ ] "Date of record" работает
  - [ ] Выбор времени работает
  - [ ] Запись обновляется в Google Sheets
- [ ] Редактирование Clock out:
  - [ ] Дата = Date записи
  - [ ] Выбор времени работает
  - [ ] Валидация Clock out > Clock in работает
  - [ ] Запись обновляется в Google Sheets
- [ ] Редактирование Total sales:
  - [ ] Ввод новой суммы работает
  - [ ] Net sales и Total made пересчитываются
  - [ ] Запись обновляется в Google Sheets

#### Навигация
- [ ] Кнопка "Назад" работает на каждом шаге
- [ ] При возврате данные не теряются
- [ ] Стек состояний работает корректно
- [ ] Можно вернуться на несколько шагов назад

#### Обработка ошибок
- [ ] Ошибки Google Sheets отлавливаются
- [ ] Пользователь получает понятное сообщение
- [ ] Ошибки логируются
- [ ] Бот не падает при ошибке

#### Логи и мониторинг
- [ ] Логи пишутся в файл
- [ ] Логи пишутся в консоль
- [ ] Логи содержат нужную информацию
- [ ] Критические ошибки отправляются админу

---

## 8. Развертывание

### 8.1 Требования к серверу

**Минимальные требования:**
- OS: Linux (Ubuntu 20.04+, Debian 11+) или Windows Server
- CPU: 1 vCore
- RAM: 512 MB
- Disk: 1 GB
- Python: 3.8+
- Network: Постоянный интернет с доступом к:
  - Telegram Bot API
  - Google Sheets API

**Рекомендуемые требования:**
- CPU: 2 vCores
- RAM: 1 GB
- Disk: 5 GB

---

### 8.2 Инструкции по развертыванию

#### Вариант 1: Запуск через systemd (Linux)

**Шаг 1: Подготовка сервера**
```bash
# Обновить систему
sudo apt update && sudo apt upgrade -y

# Установить Python и pip
sudo apt install python3 python3-pip python3-venv git -y

# Создать пользователя для бота
sudo useradd -m -s /bin/bash botuser
sudo su - botuser
```

**Шаг 2: Клонирование и настройка**
```bash
# Клонировать репозиторий (или загрузить файлы)
cd /home/botuser
git clone <repo_url> telegram_bot
cd telegram_bot

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

**Шаг 3: Конфигурация**
```bash
# Скопировать пример .env
cp .env.example .env
nano .env  # Заполнить переменные

# Загрузить credentials
nano google_sheets_credentials.json  # Вставить JSON
```

**Шаг 4: Создать systemd service**
```bash
# Выйти из botuser
exit

# Создать service файл
sudo nano /etc/systemd/system/telegram-bot.service
```

Содержимое:
```ini
[Unit]
Description=Telegram Shift Tracking Bot
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/home/botuser/telegram_bot
Environment="PATH=/home/botuser/telegram_bot/venv/bin"
ExecStart=/home/botuser/telegram_bot/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Шаг 5: Запуск**
```bash
# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable telegram-bot

# Запустить бота
sudo systemctl start telegram-bot

# Проверить статус
sudo systemctl status telegram-bot

# Посмотреть логи
sudo journalctl -u telegram-bot -f
```

---

#### Вариант 2: Запуск через Docker

**Шаг 1: Установка Docker**
```bash
# Установить Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установить Docker Compose
sudo apt install docker-compose -y

# Добавить пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker
```

**Шаг 2: Подготовка файлов**
```bash
# Клонировать репозиторий
cd /home/botuser
git clone <repo_url> telegram_bot
cd telegram_bot

# Настроить .env
cp .env.example .env
nano .env

# Загрузить credentials
nano google_sheets_credentials.json
```

**Шаг 3: Создать Docker образ**
```bash
# Собрать образ
docker-compose build

# Или использовать docker напрямую
docker build -t telegram-bot .
```

**Шаг 4: Запуск**
```bash
# Запустить через docker-compose
docker-compose up -d

# Проверить статус
docker-compose ps

# Посмотреть логи
docker-compose logs -f

# Перезапустить
docker-compose restart

# Остановить
docker-compose down
```

---

#### Вариант 3: Запуск на Windows

**Шаг 1: Установка Python**
1. Скачать Python 3.10+ с https://www.python.org/
2. Установить с опцией "Add to PATH"

**Шаг 2: Подготовка**
```powershell
# Создать директорию
mkdir C:\telegram_bot
cd C:\telegram_bot

# Распаковать файлы проекта

# Создать виртуальное окружение
python -m venv venv
venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

**Шаг 3: Конфигурация**
1. Скопировать `.env.example` в `.env`
2. Заполнить переменные
3. Поместить `google_sheets_credentials.json`

**Шаг 4: Запуск**
```powershell
# Активировать окружение
venv\Scripts\activate

# Запустить бота
python bot.py
```

**Шаг 5: Автозапуск через Task Scheduler**
1. Открыть Task Scheduler
2. Create Basic Task
3. Trigger: At startup
4. Action: Start a program
5. Program: `C:\telegram_bot\venv\Scripts\python.exe`
6. Arguments: `bot.py`
7. Start in: `C:\telegram_bot`

---

### 8.3 Мониторинг

#### Проверка работы бота

**1. Проверка процесса**
```bash
# Linux
ps aux | grep bot.py
sudo systemctl status telegram-bot

# Docker
docker-compose ps
```

**2. Проверка логов**
```bash
# Linux (systemd)
sudo journalctl -u telegram-bot -n 50

# Linux (файл)
tail -f /home/botuser/telegram_bot/bot.log

# Docker
docker-compose logs -f --tail=50
```

**3. Проверка в Telegram**
- Отправить `/start` боту
- Проверить ответ

**4. Проверка Google Sheets**
- Открыть таблицу
- Проверить последние записи

---

### 8.4 Резервное копирование

**Что нужно сохранять:**
1. `.env` файл (переменные окружения)
2. `google_sheets_credentials.json` (credentials)
3. `bot_data.pkl` (если используется persistence)
4. `bot.log` (логи, опционально)

**Скрипт резервного копирования:**
```bash
#!/bin/bash
# backup.sh

BACKUP_DIR="/backup/telegram_bot"
BOT_DIR="/home/botuser/telegram_bot"
DATE=$(date +%Y%m%d_%H%M%S)

mkdir -p "$BACKUP_DIR"

tar -czf "$BACKUP_DIR/backup_$DATE.tar.gz" \
    "$BOT_DIR/.env" \
    "$BOT_DIR/google_sheets_credentials.json" \
    "$BOT_DIR/bot_data.pkl"

# Удалить старые бэкапы (старше 30 дней)
find "$BACKUP_DIR" -name "backup_*.tar.gz" -mtime +30 -delete

echo "Backup created: backup_$DATE.tar.gz"
```

**Настроить cron:**
```bash
crontab -e

# Резервное копирование каждый день в 3:00
0 3 * * * /home/botuser/telegram_bot/backup.sh
```

---

### 8.5 Обновление бота

**Процесс обновления:**

```bash
# Остановить бота
sudo systemctl stop telegram-bot

# Сделать резервную копию
./backup.sh

# Обновить код
cd /home/botuser/telegram_bot
git pull

# Обновить зависимости (если изменились)
source venv/bin/activate
pip install -r requirements.txt --upgrade

# Запустить бота
sudo systemctl start telegram-bot

# Проверить статус
sudo systemctl status telegram-bot
```

**Для Docker:**
```bash
# Остановить
docker-compose down

# Обновить код
git pull

# Пересобрать образ
docker-compose build

# Запустить
docker-compose up -d

# Проверить логи
docker-compose logs -f
```

---

## 9. Заключение

### 9.1 Итоговая оценка

**Предоставленный код:**
- ✅ Хорошая архитектурная база
- ✅ Основной функционал реализован
- ⚠️ Требуются доработки (см. раздел 3)
- ⚠️ Нужно тестирование

**Оценка объема работ:**
- Критические исправления: 2-3 дня
- Важные улучшения: 2-3 дня
- Рефакторинг и улучшения: 2-3 дня
- Тестирование: 2-3 дня
- Документация и развертывание: 1-2 дня

**Итого:** 9-14 рабочих дней

---

### 9.2 Приоритеты

**Обязательно сделать (Must Have):**
1. ✅ Исправить формат времени (уточнить у заказчика)
2. ✅ Реализовать полное редактирование Clock in/out
3. ✅ Добавить валидацию времени (Clock out > Clock in)
4. ✅ Добавить обработку ошибок Google Sheets
5. ✅ Протестировать основной flow

**Желательно сделать (Should Have):**
1. ⚠️ Переработать систему "Назад"
2. ⚠️ Добавить проверку дубликатов продуктов
3. ⚠️ Добавить логирование
4. ⚠️ Создать README.md

**Можно сделать позже (Nice to Have):**
1. 🟢 Разделить код на модули
2. 🟢 Добавить persistence (Redis/файл)
3. 🟢 Улучшить UX (markdown, подтверждения)
4. 🟢 Настроить мониторинг

---

### 9.3 Рекомендации

**Перед началом разработки:**
1. ⚡ Уточнить у заказчика формат времени (слэши или двоеточия)
2. ⚡ Проверить, что Google Sheets API работает
3. ⚡ Создать тестовую таблицу
4. ⚡ Получить тестовый Telegram Bot Token

**Во время разработки:**
1. 🔧 Использовать git для версионирования
2. 🔧 Делать коммиты после каждой задачи
3. 🔧 Тестировать каждое изменение
4. 🔧 Вести CHANGELOG.md

**После завершения:**
1. 📋 Провести финальное тестирование
2. 📋 Написать инструкции для пользователя
3. 📋 Развернуть на production сервере
4. 📋 Настроить мониторинг

---

### 9.4 Контрольные точки

**Контрольная точка 1 (после Этапа 2):**
- [ ] Формат времени согласован
- [ ] Редактирование Clock in/out работает
- [ ] Валидация времени работает
- [ ] Обработка ошибок добавлена
- [ ] Основной flow протестирован

**Решение:** Продолжать дальше ИЛИ передать заказчику на проверку

---

**Контрольная точка 2 (после Этапа 4):**
- [ ] Код разделен на модули (опционально)
- [ ] UX улучшен
- [ ] README.md создан
- [ ] Логирование работает

**Решение:** Перейти к тестированию

---

**Контрольная точка 3 (после Этапа 5):**
- [ ] Все тесты пройдены
- [ ] Ручное тестирование завершено
- [ ] Критические баги исправлены

**Решение:** Перейти к развертыванию

---

**Контрольная точка 4 (финальная):**
- [ ] Бот развернут на production
- [ ] Мониторинг настроен
- [ ] Резервное копирование настроено
- [ ] Инструкции переданы заказчику

**Решение:** Проект завершен

---

## Приложения

### Приложение A: Пример .env файла

```env
# Telegram Bot
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Google Sheets
GOOGLE_SA_JSON=google_sheets_credentials.json
SPREADSHEET_ID=1Z7HVmO3B7d-HWU2dx5v9HD04ZpmkQIzxxCjamcbafWI
SHEET_NAME=Shifts

# Products (comma-separated)
PRODUCTS=Model A,Model B,Model C

# Rates
COMMISSION_RATE=0.20
PAYOUT_RATE=1.00

# Time
USE_FIXED_UTC_MINUS_5=true

# Monitoring (optional)
ADMIN_CHAT_ID=123456789
```

---

### Приложение B: Пример структуры Google Sheets

| ID | Date | EmployeeId | EmployeeName | Clock in | Clock out | Model A | Model B | Model C | Total sales | Net sales | Total made |
|----|------|------------|--------------|----------|-----------|---------|---------|---------|-------------|-----------|------------|
| 1 | 2025/10/15 18:30:00 | 123456789 | username | 2025/10/15 09:00:00 | 2025/10/15 17:00:00 | 150.00 | | 200.00 | 350.00 | 280.00 | 280.00 |
| 2 | 2025/10/16 19:15:00 | 987654321 | another_user | 2025/10/16 10:00:00 | 2025/10/16 18:00:00 | | 300.00 | | 300.00 | 240.00 | 240.00 |

---

### Приложение C: Полезные команды

```bash
# Проверка статуса бота
sudo systemctl status telegram-bot

# Перезапуск бота
sudo systemctl restart telegram-bot

# Просмотр логов (последние 100 строк)
sudo journalctl -u telegram-bot -n 100

# Просмотр логов в реальном времени
sudo journalctl -u telegram-bot -f

# Проверка процесса
ps aux | grep bot.py

# Проверка порта (если используется webhook)
sudo netstat -tlnp | grep python

# Проверка использования ресурсов
top -p $(pgrep -f bot.py)

# Очистка логов (осторожно!)
sudo journalctl --vacuum-time=7d
```

---

### Приложение D: Чек-лист перед запуском

**Окружение:**
- [ ] Python 3.8+ установлен
- [ ] pip установлен
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены

**Конфигурация:**
- [ ] .env файл создан и заполнен
- [ ] BOT_TOKEN корректный
- [ ] SPREADSHEET_ID корректный
- [ ] google_sheets_credentials.json загружен
- [ ] Service Account имеет доступ к таблице

**Таблица:**
- [ ] Google таблица создана
- [ ] Service Account добавлен с правами "Редактор"
- [ ] Лист называется "Shifts" (или соответствует SHEET_NAME)
- [ ] Заголовки будут созданы автоматически при первом запуске

**Безопасность:**
- [ ] .env в .gitignore
- [ ] google_sheets_credentials.json в .gitignore
- [ ] Токены и ключи не светятся в коде

**Тестирование:**
- [ ] Бот запускается без ошибок
- [ ] /start работает
- [ ] Можно создать смену
- [ ] Запись появляется в Google Sheets

---

## Контакты и Поддержка

**При возникновении проблем:**
1. Проверить логи: `sudo journalctl -u telegram-bot -n 100`
2. Проверить статус: `sudo systemctl status telegram-bot`
3. Проверить Google Sheets API доступ
4. Проверить Telegram Bot Token

**Полезные ссылки:**
- Python Telegram Bot: https://docs.python-telegram-bot.org/
- gspread: https://docs.gspread.org/
- Google Sheets API: https://developers.google.com/sheets/api

---

**Документ подготовлен:** 2025-10-15
**Версия:** 1.0
**Статус:** Готов к использованию
