# Alex12060 Telegram Bot - Руководство для Claude Code

**Проект:** Telegram бот для управления сменами сотрудников
**Сервер:** Pi4-2
**Путь на сервере:** `/home/lexun/Alex12060`
**Дата создания:** 2025-11-10

---

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Архитектура](#архитектура)
- [Управление ботом](#управление-ботом)
- [Основные компоненты](#основные-компоненты)
- [База данных и производительность](#база-данных-и-производительность)
- [Недавние изменения](#недавние-изменения)
- [Правила работы с ботом](#правила-работы-с-ботом)
- [Устранение проблем](#устранение-проблем)

---

## 🎯 Описание проекта

Alex12060 - Telegram бот для управления рабочими сменами сотрудников салона красоты. Бот интегрирован с Google Sheets для хранения данных о сменах, продажах, комиссиях и бонусах.

### Основные функции:
- ✅ Создание и редактирование смен (Clock in/out)
- ✅ Учет продаж по продуктам (Bella, Laura, Sophie, Alice, Emma, Molly)
- ✅ Автоматический расчет комиссии (base + dynamic + bonus)
- ✅ Система бонусов и рангов
- ✅ Детализация breakdown комиссии

---

## 🏗️ Архитектура

### Структура проекта:
```
Alex12060/
├── bot.py                              # Главный файл бота
├── handlers.py                         # Обработчики команд и сообщений
├── sheets_service.py                   # Интеграция с Google Sheets (1217 строк)
├── cache_manager.py                    # 🆕 Менеджер кэша (v1.1.0)
├── services.py                         # 🆕 Singleton сервисы с кэшем
├── config.py                           # Конфигурация бота
├── time_utils.py                       # Утилиты для работы со временем
├── test_cache.py                       # Тесты системы кэширования
├── venv/                               # Виртуальное окружение Python
├── bot.log                             # Логи бота
├── alex12060-bot.service               # systemd service файл
├── ANALYSIS_MULTIPLE_INSTANCES.md      # Анализ проблемы множественных экземпляров
├── CHANGELOG_COMMISSION_BREAKDOWN.md   # История изменений комиссии
├── CHANGELOG_CACHING_SYSTEM.md         # 🆕 История внедрения кэширования
├── DATABASE_ANALYSIS_DEEP_DIVE.md      # 🔬 ГЛУБОКИЙ анализ БД и оптимизация
└── CLAUDE.md                           # Этот файл
```

### Технологии:
- **Python 3.x**
- **python-telegram-bot** - библиотека для Telegram API
- **Google Sheets API** - хранение данных
- **Local Caching** - 🆕 in-memory кэш с TTL для оптимизации (v1.1.0)
- **systemd** - управление процессом

---

## 🚀 Управление ботом

### ⚠️ КРИТИЧНО ВАЖНО: Использовать ТОЛЬКО systemd!

Бот работает как systemd service. **НИКОГДА** не запускайте бот вручную через SSH!

### Команды управления:

```bash
# Запустить бота
ssh Pi4-2 "sudo systemctl start alex12060-bot"

# Остановить бота
ssh Pi4-2 "sudo systemctl stop alex12060-bot"

# Перезапустить бота
ssh Pi4-2 "sudo systemctl restart alex12060-bot"

# Статус бота
ssh Pi4-2 "sudo systemctl status alex12060-bot"

# Просмотр логов (systemd journal)
ssh Pi4-2 "sudo journalctl -u alex12060-bot -f"

# Просмотр логов бота (файл)
ssh Pi4-2 "tail -f Alex12060/bot.log"

# Проверить количество запущенных экземпляров (должно быть 1!)
ssh Pi4-2 "pgrep -c -f 'Alex12060.*python3.*bot.py'"
```

### Деплой изменений:

```bash
# 1. Скопировать изменённые файлы
scp handlers.py Pi4-2:/home/lexun/Alex12060/
scp bot.py Pi4-2:/home/lexun/Alex12060/

# 2. Перезапустить service
ssh Pi4-2 "sudo systemctl restart alex12060-bot"

# 3. Проверить статус
ssh Pi4-2 "sudo systemctl status alex12060-bot"

# 4. Проверить логи
ssh Pi4-2 "tail -20 Alex12060/bot.log"
```

### Systemd Service файл:

Расположение: `/etc/systemd/system/alex12060-bot.service`

```ini
[Unit]
Description=Alex12060 Telegram Bot
After=network.target

[Service]
Type=simple
User=lexun
WorkingDirectory=/home/lexun/Alex12060
ExecStart=/home/lexun/Alex12060/venv/bin/python3 /home/lexun/Alex12060/bot.py
Restart=always
RestartSec=10
StandardOutput=append:/home/lexun/Alex12060/bot.log
StandardError=append:/home/lexun/Alex12060/bot.log

# Security settings
NoNewPrivileges=true
PrivateTmp=true

[Install]
WantedBy=multi-user.target
```

---

## 🔧 Основные компоненты

### handlers.py

Содержит всю логику обработки команд и сообщений бота.

**Ключевые функции:**

1. **`get_commission_breakdown(employee_id, commission_pct, shift_id=None)`**
   - Рассчитывает breakdown комиссии (base + dynamic + bonus)
   - Возвращает строку вида: "15.00% (8.0% base + 6.0% dynamic + 1.0% bonus)"
   - Местоположение: handlers.py:141-198

2. **`format_shift_totals(shift_data, employee_id, shift_id=None)`**
   - Форматирует секцию "💵 Totals" с детализацией
   - Местоположение: handlers.py:204-232

3. **`format_shift_details(shift_data, employee_id, shift_id)`**
   - Форматирует полное сообщение о смене (для редактирования)
   - Включает ID, дату, сотрудника, продукты, время и итоги
   - Местоположение: handlers.py:238-281

4. **`build_summary()`**
   - Создает итоговое сообщение при создании смены
   - Использует `get_commission_breakdown()` для детализации
   - Местоположение: handlers.py:~250

**Обработчики редактирования:**
- `handle_edit_time_in()` - редактирование времени начала
- `handle_edit_time_out()` - редактирование времени окончания
- `handle_edit_total_sales_input()` - редактирование продаж

### sheets_service.py

Интеграция с Google Sheets API.

**Основные методы:**
- `get_employee_settings(employee_id)` - настройки сотрудника
- `get_shift_applied_bonuses(shift_id)` - активные бонусы смены
- `update_shift()` - обновление данных смены

---

## ⚡ Система кэширования (v1.1.0)

### 🆕 Новое! Локальное кэширование

**Дата внедрения:** 2025-11-10
**Версия:** v1.1.0 (PROMPT 2.2)
**Статус:** ✅ Production

### Что это:

Система in-memory кэширования с TTL для снижения нагрузки на Google Sheets API и улучшения производительности бота.

### Компоненты:

#### 1. `cache_manager.py` (221 строка)
- **CacheManager** - основной менеджер кэша
- **DummyCacheManager** - заглушка для обратной совместимости
- Namespace-based кэширование
- TTL support (5-15 минут в зависимости от данных)
- Статистика (hits, misses, hit rate)

#### 2. `services.py` (19 строк)
- Singleton экземпляры с включенным кэшем
- `sheets_service` - SheetsService с кэшем
- `cache_manager` - менеджер кэша

### Кэшируемые методы:

| Метод | TTL | Описание |
|-------|-----|----------|
| `get_employee_settings()` | 10 мин | Настройки сотрудников |
| `get_dynamic_rates()` | 15 мин | Динамические ставки |
| `get_ranks()` | 15 мин | Система рангов |
| `get_employee_rank()` | 5 мин | Ранги сотрудников |
| `get_shift_applied_bonuses()` | 10 мин | Бонусы смен |

### Инвалидация кэша:

- **`create_shift()`** - инвалидирует employee_settings
- **`update_shift_field()`** - инвалидирует shift
- **`update_total_sales()`** - инвалидирует shift + shift_bonuses

### Производительность:

**Ожидаемое улучшение:**
- API calls: ⬇ 40-60%
- Latency: ⬇ 30-50%
- Peak API load: ⬇ 50-70%

**Тестовые результаты:**
- Cache hit rate: 50% (в тестах)
- Latency для cache hits: ~0ms vs ~1-2s для API calls

### Команды мониторинга:

```bash
# Запуск тестов кэша
ssh Pi4-2 "cd Alex12060 && venv/bin/python3 test_cache.py"

# Проверка статистики (из Python)
from services import cache_manager
cache_manager.log_stats()

# Просмотр debug логов
ssh Pi4-2 "tail -f Alex12060/bot.log | grep -i cache"
```

### Документация:

См. **`CHANGELOG_CACHING_SYSTEM.md`** для полной информации о внедрении, архитектуре и деплое.

---

## 💾 База данных и производительность

### Текущая архитектура:

Бот использует **Google Sheets** как базу данных через API.

**Таблицы (worksheets):**
- `Shifts` - основная таблица смен (~500-1000 записей)
- `EmployeeSettings` - настройки сотрудников
- `DynamicRates` - динамические ставки комиссии
- `Ranks` - система рангов
- `EmployeeRanks` - ранги сотрудников по месяцам
- `ActiveBonuses` - активные бонусы

### Текущая производительность:

| Операция | Время | API Calls |
|----------|-------|-----------|
| Создание смены | 1.5-3.0s | 8-15 |
| Редактирование | 0.5-1.5s | 3-5 |
| Просмотр истории | 0.5-2.0s | 2-3 |

### Глубокий анализ и оптимизация:

**См. документ:** `DATABASE_ANALYSIS_DEEP_DIVE.md`

Этот документ содержит:
- 🔬 Детальный анализ узких мест производительности
- 📊 Измерения латентности и API usage
- 💡 Три решения для оптимизации:
  - **Решение #1:** Локальное кэширование (улучшение на 40%, 2-4 часа внедрения)
  - **Решение #2:** Гибридная архитектура SQLite + Sheets (улучшение на 90%, 1-2 дня)
  - **Решение #3:** Полная миграция на PostgreSQL (улучшение на 98%, 1-2 недели)
- 📈 Сравнительную таблицу решений
- 🎯 Поэтапный план внедрения
- 💻 Примеры кода для каждого решения

**Рекомендация:** Начать с Решения #1 (кэширование) для быстрого улучшения, затем при необходимости перейти к Решению #2 (гибрид).

### Когда переходить к оптимизации:

```
Фаза 1 (Кэширование) - если:
- Количество смен > 1000
- Появляются rate limit ошибки
- Латентность > 3 секунд

Фаза 2 (Гибрид) - если:
- Количество смен > 10,000
- Пользователей > 10 одновременно
- Нужна бизнес-аналитика
```

---

## 📝 Недавние изменения

### 2025-11-10: Система локального кэширования (v1.1.0) ⚡ НОВОЕ!

**Что изменилось:**
1. ✅ Внедрена система in-memory кэширования с TTL
2. ✅ Создан `cache_manager.py` (221 строка) - менеджер кэша
3. ✅ Создан `services.py` (19 строк) - singleton сервисы
4. ✅ Добавлено кэширование в 5 основных методов `sheets_service.py`
5. ✅ Автоматическая инвалидация при изменении данных
6. ✅ Обновлён `handlers.py` для использования singleton с кэшем

**Производительность:**
```
Ожидаемое улучшение:
- API calls: ⬇ 40-60%
- Latency: ⬇ 30-50%
- Peak API load: ⬇ 50-70%
```

**Тестирование:**
- ✅ Все тесты пройдены локально
- ✅ Все тесты пройдены на сервере
- ✅ Деплой в production успешен

**Статус:** ✅ **PRODUCTION READY**

**Подробности:** см. `CHANGELOG_CACHING_SYSTEM.md`

---

### 2025-11-10: Детализация комиссии и унификация сообщений

**Что изменилось:**
1. ✅ Добавлен breakdown комиссии с разбивкой на base + dynamic + bonus
2. ✅ Унифицированы сообщения при редактировании смены
3. ✅ Исправлены ошибки с типами данных (string → float)

**Формат комиссии:**
```
Было:    Commission %: 15.0%
Стало:   Commission %: 15.00% (8.0% base + 6.0% dynamic + 1.0% bonus)
```

**Подробности:** см. `CHANGELOG_COMMISSION_BREAKDOWN.md`

### 2025-11-10: Проблема множественных экземпляров (РЕШЕНА)

**Проблема:** Запускалось 5-7 экземпляров бота одновременно, вызывая конфликты Telegram API.

**Причина:** Неправильное использование SSH команд с `nohup ... &`, создающих зомби bash-процессы.

**Решение:** Создан systemd service для правильного управления процессом.

**Подробности:** см. `ANALYSIS_MULTIPLE_INSTANCES.md`

---

## ⚠️ Правила работы с ботом

### ✅ МОЖНО и НУЖНО:

```bash
# Управление через systemd
sudo systemctl start/stop/restart alex12060-bot
sudo systemctl status alex12060-bot
sudo journalctl -u alex12060-bot -f

# Просмотр логов
tail -f /home/lexun/Alex12060/bot.log

# Проверка процессов
pgrep -af 'Alex12060.*python3.*bot.py'
```

### ❌ НЕЛЬЗЯ и ОПАСНО:

```bash
# ❌ НЕ запускать вручную через SSH!
ssh Pi4-2 "cd Alex12060 && nohup venv/bin/python3 bot.py &"
ssh Pi4-2 "cd Alex12060 && venv/bin/python3 bot.py > bot.log 2>&1 &"

# ❌ НЕ использовать nohup для запуска!
nohup venv/bin/python3 bot.py &

# ❌ НЕ запускать несколько экземпляров!
# Это вызовет "Conflict: terminated by other getUpdates request"
```

**Почему нельзя:**
- Создаются зомби bash-процессы
- Процессы не завершаются при закрытии SSH
- Накапливаются множественные экземпляры
- Telegram API конфликты
- Невозможно отследить статус

---

## 🔍 Устранение проблем

### Проблема: Бот не отвечает на сообщения

**Проверить:**
```bash
# 1. Статус service
ssh Pi4-2 "sudo systemctl status alex12060-bot"

# 2. Количество экземпляров (должно быть 1!)
ssh Pi4-2 "pgrep -c -f 'Alex12060.*python3.*bot.py'"

# 3. Логи
ssh Pi4-2 "tail -50 Alex12060/bot.log"

# 4. Ошибки в systemd
ssh Pi4-2 "sudo journalctl -u alex12060-bot -n 50"
```

**Решение:**
```bash
# Перезапустить service
ssh Pi4-2 "sudo systemctl restart alex12060-bot"
```

### Проблема: Множественные экземпляры

**Диагностика:**
```bash
# Проверить количество процессов
ssh Pi4-2 "pgrep -af 'Alex12060.*python3.*bot.py'"

# Должен быть ОДИН процесс, управляемый systemd
```

**Решение:**
```bash
# 1. Убить все процессы
ssh Pi4-2 "killall -9 python3"

# 2. Запустить через systemd
ssh Pi4-2 "sudo systemctl start alex12060-bot"

# 3. Проверить
ssh Pi4-2 "pgrep -c -f 'Alex12060.*python3.*bot.py'"
# Должно вывести: 1
```

### Проблема: "Conflict: terminated by other getUpdates request"

**Причина:** Запущено несколько экземпляров бота.

**Решение:** см. "Множественные экземпляры" выше.

### Проблема: TypeError с commission_pct

**Ошибка:**
```
ValueError: Unknown format code 'f' for object of type 'str'
```

**Причина:** Google Sheets возвращает числа как строки.

**Решение:** В handlers.py:159 и :217 добавлено преобразование:
```python
commission_pct = float(commission_pct)
```

### Проблема: Бот не стартует после перезагрузки

**Проверить:**
```bash
# Включен ли autostart
ssh Pi4-2 "sudo systemctl is-enabled alex12060-bot"
# Должно вывести: enabled
```

**Решение:**
```bash
# Включить autostart
ssh Pi4-2 "sudo systemctl enable alex12060-bot"
```

---

## 📊 Мониторинг

### Чек-лист здоровья бота:

```bash
# 1. Service активен и работает
ssh Pi4-2 "sudo systemctl is-active alex12060-bot"
# Ожидается: active

# 2. Только ОДИН экземпляр
ssh Pi4-2 "pgrep -c -f 'Alex12060.*python3.*bot.py'"
# Ожидается: 1

# 3. Нет ошибок в логах (последние 10 строк)
ssh Pi4-2 "tail -10 Alex12060/bot.log | grep -i error"
# Ожидается: пусто или нет критичных ошибок

# 4. Процесс не перезапускался недавно
ssh Pi4-2 "sudo systemctl status alex12060-bot | grep Active"
# Проверить, что uptime разумный
```

---

## 🔐 Безопасность

### Учетные данные:

**НЕ хранятся в репозитории!**

- Google Sheets credentials: `credentials.json` (на сервере)
- Telegram Bot Token: в переменных окружения или конфиге

### Права доступа:

```bash
# Service файл
/etc/systemd/system/alex12060-bot.service: 644 (root:root)

# Рабочая директория
/home/lexun/Alex12060: 755 (lexun:lexun)

# Логи
/home/lexun/Alex12060/bot.log: 644 (lexun:lexun)
```

---

## 📞 Контакты и поддержка

При возникновении проблем:
1. Проверить логи бота
2. Проверить статус systemd service
3. Проверить количество запущенных экземпляров
4. Просмотреть `ANALYSIS_MULTIPLE_INSTANCES.md` для понимания распространенных проблем

---

## 📚 Дополнительные документы

- **DATABASE_ANALYSIS_DEEP_DIVE.md** - 🔬 глубокий анализ архитектуры БД, узких мест производительности и план оптимизации (UltraThink Mode)
- **ANALYSIS_MULTIPLE_INSTANCES.md** - глубокий анализ проблемы множественных экземпляров
- **CHANGELOG_COMMISSION_BREAKDOWN.md** - история изменений детализации комиссии
- **test_commission_breakdown.py** - тесты breakdown функции
- **demo_shift_message.py** - демонстрация формата сообщений

---

**Последнее обновление:** 2025-11-10
**Версия:** 1.0
**Статус:** Production-ready
