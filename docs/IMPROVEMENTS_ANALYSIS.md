# Анализ Доработок Telegram Shift Tracking Bot

## Дата создания: 2025-10-27
## Статус: В разработке

---

## 1. Текущая Структура Системы

### 1.1. Существующая таблица Shifts

**Текущие столбцы:**
```
ID | Date | EmployeeId | EmployeeName | Clock in | Clock out | Model A | Model B | Model C | Total sales | Net sales | Total made
```

**Текущие расчеты:**
```python
Total sales = sum(Model A, Model B, Model C, ...)
Net sales = Total sales × (1 - COMMISSION_RATE)  # COMMISSION_RATE = 0.20
Total made = Net sales × PAYOUT_RATE              # PAYOUT_RATE = 1.00
```

**Текущая логика:**
- Фиксированная ставка комиссии 20% для всех
- Заработок = Net sales (без учета почасовой оплаты)
- Нет системы рангов и бонусов

---

## 2. Анализ Требований по Доработкам

### 2.1. Новые столбцы в таблице Shifts

#### Пункт 1: Worked hours/shift
**Требование:**
- Столбец "Worked hours/shift"
- Рассчитывается из разницы "Clock out" - "Clock in"

**Формула:**
```python
worked_hours = (clock_out_datetime - clock_in_datetime).total_seconds() / 3600
```

**Формат:** Число с плавающей точкой (например, 8.5 для 8 часов 30 минут)

---

#### Пункт 2: Total per hour
**Требование:**
- Столбец "Total per hour"
- Оплата за отработанные часы
- Зависит от индивидуальной почасовой ставки сотрудника

**Формула:**
```python
total_per_hour = worked_hours × hourly_wage
```

**Источник данных:**
- `hourly_wage` берется из таблицы **Employees** по EmployeeId

---

#### Пункт 3: % (процентная ставка)
**Требование:**
- Столбец "%"
- Процент с продаж = Базовый % + Динамический %
- Базовый % индивидуален для каждого сотрудника
- Динамический % зависит от Total sales за текущий день

**Формула:**
```python
base_commission = получить из таблицы Employees по EmployeeId
dynamic_commission = получить из таблицы DynamicRates по Total sales
total_commission = base_commission + dynamic_commission
```

**Пример:**
- Базовая ставка: 8%
- Total sales за день: $500 → динамическая ставка: 2%
- Итоговая ставка: 10%

**Особенность:**
- "Total sales за текущий день" = сумма всех Total sales данного сотрудника за календарный день (день серверного времени из поля Date)

---

#### Пункт 4: Commissions
**Требование:**
- Столбец "Commissions"
- Комиссия с продаж

**Формула:**
```python
commissions = Net sales × (total_commission_rate / 100)
```

**Примечание:**
- Net sales остается как есть: `Total sales × (1 - 0.20)` (вычет 20% платформы)
- Commissions = доля сотрудника от Net sales

---

#### Пункт 5: Total made (обновленный)
**Требование:**
- Обновить расчет столбца "Total made"

**Новая формула:**
```python
Total made = Commissions + Total per hour
```

**Было:**
```python
Total made = Net sales × PAYOUT_RATE
```

---

### 2.2. Новая структура таблицы Shifts

**ИТОГОВАЯ СТРУКТУРА:**
```
ID | Date | EmployeeId | EmployeeName | Clock in | Clock out |
Worked hours/shift | Model A | Model B | Model C | Total sales |
Net sales | % | Commissions | Total per hour | Total made
```

**Порядок столбцов:**
1. ID
2. Date
3. EmployeeId
4. EmployeeName
5. Clock in
6. Clock out
7. **Worked hours/shift** (новый)
8. Model A
9. Model B
10. Model C
11. Total sales
12. Net sales
13. **%** (новый)
14. **Commissions** (новый)
15. **Total per hour** (новый)
16. Total made (обновленный расчет)

---

## 3. Новые Таблицы Google Sheets

### 3.1. Таблица Employees (Пункт 6)

**Назначение:** Хранение индивидуальных настроек сотрудников

**Структура:**
```
EmployeeId | EmployeeName | Hourly wage | Sales commission
```

**Описание столбцов:**
- `EmployeeId` (число): Telegram user ID
- `EmployeeName` (текст): Username или имя сотрудника
- `Hourly wage` (число): Почасовая ставка в $ (например, 5.00)
- `Sales commission` (число): Базовая процентная ставка (например, 8 для 8%)

**Пример данных:**
```
120962578 | StepunR | 5.00 | 8
123456789 | JohnDoe | 6.50 | 10
```

**Особенности:**
- Первичный ключ: EmployeeId
- При первом создании смены сотрудника, если его нет в таблице, нужно добавить с дефолтными значениями
- Администратор может вручную редактировать ставки

---

### 3.2. Таблица DynamicRates (Пункт 3)

**Назначение:** Условия для динамической процентной ставки

**Структура:**
```
Min sales | Max sales | Bonus rate
```

**Описание столбцов:**
- `Min sales` (число): Минимальная сумма продаж (включительно)
- `Max sales` (число): Максимальная сумма продаж (не включительно)
- `Bonus rate` (число): Дополнительный процент (например, 2 для +2%)

**Пример данных:**
```
0      | 200   | 0
200    | 400   | 1
400    | 600   | 2
600    | 1000  | 3
1000   | 99999 | 5
```

**Логика применения:**
- Найти запись где `Min sales <= Total_sales_за_день < Max sales`
- Вернуть `Bonus rate` из этой записи
- Если не найдено, вернуть 0

**Вопрос для обсуждения:**
> Клиент предложил хранить в таблице, но готов обсудить реализацию в коде.
>
> **Рекомендация:** Хранить в таблице для гибкости (можно менять без правки кода).

---

### 3.3. Таблица Ranks (Пункт 8)

**Назначение:** Ранговая система с условиями и бонусами

**Структура:**
```
Rank name | Icon | Min sales | Max sales | Bonus 1 | Bonus 2 | Bonus 3
```

**Описание столбцов:**
- `Rank name` (текст): Название ранга (например, "Hustler")
- `Icon` (текст): Эмодзи (например, "💪")
- `Min sales` (число): Минимальная сумма продаж за месяц
- `Max sales` (число): Максимальная сумма продаж за месяц (включительно)
- `Bonus 1`, `Bonus 2`, `Bonus 3` (текст): Описания бонусов

**Пример данных:**
```
Rookie          | 🧑‍🚀 | 0     | 1999  | -                                  | -                                      | -
Hustler         | 💪   | 2000  | 3999  | Flat bonus +10$                    | +1% Commission on your next shift      | +1% Commission from the chatter from previous shift
Closer          | 💼   | 4000  | 6999  | Flat bonus +25$                    | +2% Commission on your next shift      | Flat bonus +15$
Shark           | 🦈   | 7000  | 9999  | Flat bonus +50$                    | Pick a Date for Paid Day Off           | +3% Commission on your next shift
King of Greed   | 💀   | 10000 | 14999 | Flat bonus +100$                   | +2% from sales of ALL other chatters on your model that day(next shift) | Telegram Premium for 3months
Chatting God    | 🔥   | 15000 | 99999 | Flat bonus +200$                   | Double commission on your next shift   | Flat bonus +125$
```

**Логика применения:**
- Total sales за месяц рассчитывается как сумма всех Total sales сотрудника за текущий календарный месяц
- Ранг определяется по условию: `Min sales <= Total_sales_месяц <= Max sales`
- При достижении нового ранга (проверка после каждого завершения смены):
  - Отправить сообщение о достижении
  - Случайно выбрать один из 3 бонусов
  - Сохранить бонус в историю

**Сброс рангов:**
- 1-го числа каждого месяца все ранги сбрасываются
- Статистика по месяцам сохраняется

---

### 3.4. Таблица EmployeeBonuses

**Назначение:** История выданных бонусов

**Структура:**
```
ID | Date | EmployeeId | EmployeeName | Rank | Bonus | Status
```

**Описание столбцов:**
- `ID` (число): Автоинкремент
- `Date` (дата): Дата получения бонуса
- `EmployeeId` (число): Telegram user ID
- `EmployeeName` (текст): Username
- `Rank` (текст): Название ранга (например, "Hustler 💪")
- `Bonus` (текст): Описание бонуса
- `Status` (текст): Статус бонуса (Pending / Applied / Expired)

**Пример данных:**
```
1 | 2025/10/15 18:30:00 | 120962578 | StepunR | Hustler 💪 | Flat bonus +10$ | Applied
2 | 2025/10/20 20:15:00 | 120962578 | StepunR | Closer 💼  | +2% Commission on your next shift | Pending
```

**Типы бонусов и их применение:**

1. **Flat bonus +X$**
   - Просто информируем сотрудника
   - Администратор вручную добавит к выплате

2. **+X% Commission on your next shift**
   - При создании следующей смены добавить X% к процентной ставке
   - После применения пометить как Applied

3. **+X% Commission from the chatter from previous shift**
   - Сложный бонус, требует уточнения логики
   - Возможно, применяется к предыдущей смене (перерасчет?)

4. **Double commission on your next shift**
   - При создании следующей смены умножить % на 2
   - После применения пометить как Applied

5. **Pick a Date for Paid Day Off**
   - Информационный бонус для администратора

6. **Telegram Premium for 3 months**
   - Информационный бонус для администратора

7. **+2% from sales of ALL other chatters on your model that day(next shift)**
   - Сложный бонус, требует детального обсуждения логики

---

## 4. Новый Функционал Бота

### 4.1. Кнопка "Statistics" (Пункт 7)

**Расположение:** Главное меню бота (вместе с Create shift / Edit shift)

**Отображаемая информация:**

#### a) Rank (ранг сотрудника)
**Источник:** Таблица Ranks
**Расчет:**
- Получить сумму Total sales сотрудника за текущий календарный месяц
- Найти соответствующий ранг по условиям Min/Max sales
- Показать: `Rank name + Icon`

**Пример:**
```
🏆 Your Rank: Hustler 💪
```

---

#### b) Total sales за месяц
**Источник:** Таблица Shifts
**Расчет:**
- Фильтр: EmployeeId = текущий пользователь
- Фильтр: Месяц из поля Date = текущий месяц
- Сумма: SUM(Total sales)

**Пример:**
```
💰 Total sales this month: $2,350.00
```

---

#### c) Total made за период с последнего "pay day"
**Источник:** Таблица Shifts
**Расчет:**
- Определить последний pay day (1-го или 15-го числа текущего месяца)
- Если сегодня до 15-го → последний pay day = 1-го текущего месяца
- Если сегодня после 15-го → последний pay day = 15-го текущего месяца
- Фильтр: EmployeeId = текущий пользователь
- Фильтр: Date >= последний pay day
- Сумма: SUM(Total made)

**Пример:**
```
💵 Total made since last pay day: $1,125.50
```

---

#### d) Pay day (ближайший день выплат)
**Расчет:**
- Если сегодня до 15-го → следующий pay day = 15-го текущего месяца
- Если сегодня >= 15-го → следующий pay day = 1-го следующего месяца

**Пример:**
```
📅 Next pay day: November 1, 2025
```

---

**Пример полного вывода Statistics:**
```
📊 Your Statistics

🏆 Rank: Hustler 💪

💰 Total sales this month: $2,350.00

💵 Total made since last pay day: $1,125.50
(from October 15 to today)

📅 Next pay day: November 1, 2025
```

---

### 4.2. Кнопка "Ranks" (Пункт 9)

**Расположение:** Главное меню бота

**Назначение:** Информация о ранговой системе

**Содержание:**
Вывести все ранги с их условиями и бонусами из таблицы Ranks

**Формат вывода:**
```
🏆 Rank System

🧑‍🚀 Rookie ($0 – $1,999)
No bonuses — this is the baseline. Everyone starts here.

💪 Hustler ($2,000 – $3,999)
Possible bonuses (random):
1. Flat bonus +10$
2. +1% Commission on your next shift
3. +1% Commission from the chatter from previous shift

💼 Closer ($4,000 – $6,999)
Possible bonuses (random):
1. Flat bonus +25$
2. +2% Commission on your next shift
3. Flat bonus +15$

🦈 Shark ($7,000 – $9,999)
Possible bonuses (random):
1. Flat bonus +50$
2. Pick a Date for Paid Day Off
3. +3% Commission on your next shift

💀 King of Greed ($10,000 – $14,999)
Possible bonuses (random):
1. Flat bonus +100$
2. +2% from sales of ALL other chatters on your model that day(next shift)
3. Telegram Premium for 3 months

🔥 Chatting God ($15,000+)
Possible bonuses (random):
1. Flat bonus +200$
2. Double commission on your next shift
3. Flat bonus +125$
```

---

### 4.3. Система Уведомлений о Достижении Ранга (Пункт 8)

**Триггер:** После успешного создания смены

**Логика:**
1. Рассчитать Total sales за текущий месяц (включая только что созданную смену)
2. Определить текущий ранг
3. Проверить, был ли предыдущий ранг меньше
4. Если ранг повысился:
   - Случайно выбрать один из 3 бонусов ранга
   - Показать сообщение о достижении
   - Сохранить бонус в таблицу EmployeeBonuses

**Формат сообщения:**
```
🎉 Congratulations!

Now you're {Chatting God 🔥}

You're really killing it!

As bonus you get:
{Flat bonus +200$}
```

**Особенности:**
- Уведомление показывается только при повышении ранга
- При понижении (если это возможно) уведомление не показывается
- Бонус выбирается случайно (random) из 3 вариантов

---

## 5. Изменения в Логике Создания Смены

### 5.1. Расширенная последовательность расчетов

**При создании смены (handle_finish_shift):**

1. **Получить индивидуальные ставки сотрудника:**
   ```python
   employee_data = sheets.get_employee_data(employee_id)
   hourly_wage = employee_data['Hourly wage']
   base_commission = employee_data['Sales commission']
   ```

2. **Рассчитать отработанные часы:**
   ```python
   worked_hours = (clock_out - clock_in).total_seconds() / 3600
   ```

3. **Рассчитать почасовую оплату:**
   ```python
   total_per_hour = worked_hours * hourly_wage
   ```

4. **Рассчитать Total sales за день (включая текущую смену):**
   ```python
   current_date = Date поля (день без времени)
   total_sales_today = sheets.get_total_sales_for_date(employee_id, current_date)
   ```

5. **Получить динамическую ставку:**
   ```python
   dynamic_rate = sheets.get_dynamic_rate(total_sales_today)
   ```

6. **Рассчитать итоговую процентную ставку:**
   ```python
   total_commission_rate = base_commission + dynamic_rate
   ```

7. **Рассчитать остальные поля:**
   ```python
   total_sales = sum(products.values())
   net_sales = total_sales * (1 - 0.20)  # Вычет 20% платформы
   commissions = net_sales * (total_commission_rate / 100)
   total_made = commissions + total_per_hour
   ```

8. **Сохранить в Google Sheets**

9. **Проверить достижение ранга:**
   ```python
   check_and_notify_rank_achievement(employee_id, shift_id)
   ```

---

### 5.2. Новая структура данных для создания смены

```python
shift_data = {
    "date": format_dt(now_et()),
    "employee_id": employee_id,
    "employee_name": employee_name,
    "clock_in": clock_in_str,
    "clock_out": clock_out_str,
    "worked_hours": worked_hours,
    "products": products_dict,
    "total_sales": total_sales,
    "net_sales": net_sales,
    "commission_rate": total_commission_rate,
    "commissions": commissions,
    "total_per_hour": total_per_hour,
    "total_made": total_made,
}
```

---

## 6. Изменения в Логике Редактирования Смены

### 6.1. Редактирование Clock in

**Текущее поведение:** Обновляет только поле Clock in

**Новое поведение:**
- Обновить Clock in
- Пересчитать Worked hours/shift
- Пересчитать Total per hour
- Пересчитать Total made

---

### 6.2. Редактирование Clock out

**Текущее поведение:** Обновляет только поле Clock out

**Новое поведение:**
- Обновить Clock out
- Пересчитать Worked hours/shift
- Пересчитать Total per hour
- Пересчитать Total made

---

### 6.3. Редактирование Total sales

**Текущее поведение:**
- Обновляет Total sales
- Пересчитывает Net sales, Total made по старым формулам

**Новое поведение:**
- Обновить Total sales
- Пересчитать Net sales = Total sales × 0.80
- Получить индивидуальные ставки сотрудника
- Рассчитать Total sales за день (с учетом новой суммы)
- Получить динамическую ставку
- Рассчитать итоговую процентную ставку (%)
- Пересчитать Commissions = Net sales × (% / 100)
- Пересчитать Total made = Commissions + Total per hour

**ВАЖНО:** При редактировании Total sales может измениться процентная ставка (%), если изменилась сумма продаж за день!

---

## 7. Технические Детали Реализации

### 7.1. Изменения в config.py

**Добавить:**
```python
# New sheet names
EMPLOYEES_SHEET = "Employees"
DYNAMIC_RATES_SHEET = "DynamicRates"
RANKS_SHEET = "Ranks"
EMPLOYEE_BONUSES_SHEET = "EmployeeBonuses"

# Default values for new employees
DEFAULT_HOURLY_WAGE = 5.00
DEFAULT_BASE_COMMISSION = 8.0
```

**Добавить новые состояния FSM:**
```python
SHOW_STATISTICS = 16
SHOW_RANKS = 17
```

---

### 7.2. Изменения в sheets_service.py

**Новые методы:**

```python
def ensure_all_sheets(self) -> None:
    """Ensure all required sheets exist with correct headers."""

def get_or_create_employee(self, employee_id: int, employee_name: str) -> Dict:
    """Get employee data or create with defaults."""

def get_dynamic_rate(self, total_sales_today: Decimal) -> Decimal:
    """Get dynamic commission rate based on today's sales."""

def get_total_sales_for_date(self, employee_id: int, date: datetime.date) -> Decimal:
    """Get total sales for employee on specific date."""

def get_total_sales_for_month(self, employee_id: int, year: int, month: int) -> Decimal:
    """Get total sales for employee in specific month."""

def get_rank_by_sales(self, total_sales_month: Decimal) -> Optional[Dict]:
    """Get rank information based on monthly sales."""

def get_all_ranks(self) -> List[Dict]:
    """Get all ranks for display."""

def save_bonus(self, employee_id: int, employee_name: str, rank: str, bonus: str) -> int:
    """Save employee bonus to history."""

def get_pending_bonuses(self, employee_id: int) -> List[Dict]:
    """Get pending bonuses for employee."""

def mark_bonus_applied(self, bonus_id: int) -> bool:
    """Mark bonus as applied."""

def recalculate_shift(self, shift_id: int) -> bool:
    """Recalculate all fields for a shift after editing."""
```

---

### 7.3. Изменения в keyboards.py

**Обновить:**
```python
def start_menu_keyboard() -> InlineKeyboardMarkup:
    """Start menu with Create, Edit, Statistics, Ranks buttons."""
    keyboard = [
        [InlineKeyboardButton("Create shift", callback_data="CREATE_SHIFT")],
        [InlineKeyboardButton("Edit shift", callback_data="EDIT_SHIFT")],
        [InlineKeyboardButton("📊 Statistics", callback_data="STATISTICS")],
        [InlineKeyboardButton("🏆 Ranks", callback_data="RANKS")],
    ]
    return InlineKeyboardMarkup(keyboard)
```

---

### 7.4. Изменения в handlers.py

**Новые обработчики:**

```python
async def show_statistics(update: Update, context: ContextTypes.DEFAULT_TYPE) -> int:
    """Show employee statistics."""

async def show_ranks(update: Update, context: ContextTypes.DEFAULT_TYPE) -> int:
    """Show ranks information."""

async def check_and_notify_rank_achievement(
    update: Update,
    context: ContextTypes.DEFAULT_TYPE,
    employee_id: int,
    shift_id: int
) -> None:
    """Check if employee achieved new rank and notify."""
```

**Обновить:**
- `handle_finish_shift()` - добавить новые расчеты и проверку рангов
- `handle_edit_time_in()` - добавить пересчет
- `handle_edit_time_out()` - добавить пересчет
- `handle_edit_total_sales_input()` - обновить логику пересчета

---

### 7.5. Изменения в bot.py

**Обновить ConversationHandler states:**
```python
states={
    START: [...],
    ...
    SHOW_STATISTICS: [CallbackQueryHandler(handle_callback_query)],
    SHOW_RANKS: [CallbackQueryHandler(handle_callback_query)],
}
```

---

## 8. Вопросы для Уточнения

### 8.1. Критические вопросы

1. **Динамическая ставка - хранение:**
   - В таблице DynamicRates (рекомендуется)
   - ИЛИ в коде бота
   - **Решение:** Ждем ответа клиента

2. **Бонус "+X% Commission from the chatter from previous shift":**
   - Непонятна логика применения
   - Нужно уточнение у клиента

3. **Бонус "+2% from sales of ALL other chatters on your model that day(next shift)":**
   - Требует детального понимания бизнес-логики
   - Как определить "other chatters on your model"?
   - Нужны поля для связи сотрудников с моделями?

4. **Применение бонусов "на следующую смену":**
   - Автоматически применять при создании?
   - ИЛИ показывать уведомление и давать выбор?
   - **Рекомендация:** Автоматически применять

5. **Понижение ранга:**
   - Возможно ли понижение ранга при уменьшении Total sales за месяц (например, при редактировании)?
   - Нужно ли уведомлять?

---

### 8.2. Дополнительные вопросы

6. **Редактирование сотрудников:**
   - Нужна ли кнопка в боте для просмотра/изменения своих ставок?
   - ИЛИ только администратор редактирует в таблице?

7. **Валидация при создании смены:**
   - Что делать, если у сотрудника нет записи в Employees?
   - Создать автоматически с дефолтными значениями?
   - **Рекомендация:** Создать автоматически

8. **Формат отображения Worked hours:**
   - В часах с десятичной дробью (8.5)?
   - ИЛИ в формате ЧЧ:ММ (8:30)?
   - **Рекомендация:** Десятичная дробь для простоты расчетов

---

## 9. План Реализации

### Фаза 1: Структура данных (2-3 часа)
1. ✅ Создать документ анализа (этот файл)
2. Обновить config.py с новыми настройками
3. Создать структуры таблиц в sheets_service.py
4. Реализовать методы для работы с новыми таблицами
5. Тестировать создание таблиц

### Фаза 2: Расчеты при создании смены (2-3 часа)
6. Обновить логику create_shift() в sheets_service.py
7. Обновить handle_finish_shift() в handlers.py
8. Добавить все новые расчеты
9. Тестировать создание смен

### Фаза 3: Расчеты при редактировании (1-2 часа)
10. Реализовать recalculate_shift() в sheets_service.py
11. Обновить handle_edit_time_in/out()
12. Обновить handle_edit_total_sales_input()
13. Тестировать редактирование

### Фаза 4: Ранговая система (2-3 часа)
14. Реализовать get_rank_by_sales() и связанные методы
15. Реализовать check_and_notify_rank_achievement()
16. Добавить случайный выбор бонуса
17. Реализовать сохранение бонусов
18. Тестировать систему рангов

### Фаза 5: Новые кнопки (1-2 часа)
19. Обновить keyboards.py
20. Реализовать show_statistics()
21. Реализовать show_ranks()
22. Обновить bot.py с новыми обработчиками
23. Тестировать кнопки

### Фаза 6: Итоговое тестирование (1-2 часа)
24. Полное тестирование всех сценариев
25. Проверка граничных случаев
26. Обновление CLAUDE.md документации
27. Финальная проверка

**Общее время:** 9-15 часов работы

---

## 10. Риски и Ограничения

### 10.1. Риски

1. **Производительность Google Sheets API:**
   - Множество запросов для расчета Total sales за день/месяц
   - Может быть медленно при большом объеме данных
   - **Митигация:** Кэширование, batch операции

2. **Атомарность операций:**
   - Между получением данных и сохранением может создаться другая смена
   - Расчеты могут быть неточными
   - **Митигация:** Документировать ограничение

3. **Сложность бонусов:**
   - Некоторые бонусы требуют сложной логики
   - Возможны ошибки в реализации
   - **Митигация:** Детальное обсуждение с клиентом

### 10.2. Ограничения

1. **Нет транзакций:**
   - Google Sheets API не поддерживает транзакции
   - Возможны race conditions при одновременных операциях

2. **Ручное управление ставками:**
   - Администратор должен вручную редактировать таблицу Employees
   - Нет встроенного интерфейса для изменения ставок

3. **Сброс рангов 1-го числа:**
   - Требует запуска бота или периодических проверок
   - ИЛИ просто пересчитывать ранг при каждом запросе Statistics

---

## 11. Следующие Шаги

1. **Получить ответы на вопросы раздела 8**
2. **Согласовать план реализации**
3. **Начать Фазу 1: Структура данных**

---

## Дополнительные Замечания

- Все расчеты с деньгами использовать `Decimal` для точности
- Логировать все важные операции (создание смены, достижение ранга, применение бонуса)
- Форматирование сумм: всегда 2 знака после запятой (например, 1234.56)
- Временная зона: America/New_York (как в текущей реализации)
- Все сообщения пользователю на английском языке

---

**Документ подготовлен:** 2025-10-27
**Автор:** Claude Code
**Версия:** 1.0
