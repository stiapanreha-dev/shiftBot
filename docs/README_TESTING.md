# Автоматизированное тестирование бота

> **📁 Структура тестов обновлена!** Все тесты перемещены в каталог `tests/`. См. [tests/README.md](tests/README.md) для полной документации.

## 📋 Доступные тесты

### ✅ test_scenario_2_1.py - Тест создания смены с одним продуктом

**Что тестирует:**
- Полный flow создания смены через бота
- Взаимодействие с пользователем (кнопки, ввод текста)
- Сохранение данных в Google Sheets
- Корректность всех расчетов

**Как запустить:**

```bash
python3 tests/integration/test_scenario_2_1.py
```

**Что происходит:**

1. **Имитация пользователя:** Тест создает mock объекты Telegram API
2. **Полный flow:**
   - `/start` → Главное меню
   - `Create shift` → Выбор даты
   - `Server date` → Выбор времени начала
   - `9 AM` → Выбор времени окончания
   - `5 PM` → Выбор продукта
   - `Model A` → Ввод суммы
   - `500` → Завершение
   - `Finish shift` → Сохранение

3. **Проверка результатов:**
   - Смена создана в Google Sheets ✓
   - Все поля заполнены корректно ✓
   - Расчеты правильные ✓

### ✅ test_scenario_2_2.py - Тест создания смены с несколькими продуктами

**Что тестирует:**
- Добавление 3 продуктов (Model A, Model B, Model C)
- Работа кнопки "Add model"
- Корректный расчет Total sales как суммы всех продуктов
- Динамическая ставка для больших сумм

**Как запустить:**

```bash
python3 tests/integration/test_scenario_2_2.py
```

**Пример вывода (успешный тест):**

```
======================================================================
✅ TEST 2.2 PASSED - All verifications successful!
======================================================================

📋 TEST RESULTS SUMMARY
Status: PASS ✅
Shift ID: 11

Verifications:
  ✓ Worked hours: 8.0 (expected 8.0)
  ✓ Total sales: 1000.0 (expected 1000.0)
  ✓ Net sales: 800.0 (expected 800.0)
  ✓ Total per hour: 120.0 (expected 120.0)
  ✓ Commissions: 96.0 (expected 96.0)
  ✓ Total made: 216.0 (expected 216.0)
  ✓ Model A amount: 500.0 (expected 500.0)
  ✓ Model B amount: 300.0 (expected 300.0)
  ✓ Model C amount: 200.0 (expected 200.0)
```

---

## 🔧 Как работает тестирование

### Архитектура теста

```python
class BotTestSimulator:
    """Симулирует взаимодействие с ботом."""

    def _create_mock_update(self, callback_data):
        """Создает mock Update объект (имитирует нажатие кнопки)."""

    def _create_mock_context(self):
        """Создает mock Context объект (хранит состояние)."""

    async def run_test(self):
        """Выполняет полный тест сценария."""
```

### Mock объекты

Тест использует `unittest.mock` для имитации Telegram API:

- **Update** - входящее сообщение/callback от пользователя
- **Context** - контекст бота (user_data, bot)
- **CallbackQuery** - нажатие inline кнопки
- **Message** - текстовое сообщение

### Проверки (Assertions)

Тест проверяет:

1. **Состояния FSM:** Каждый handler возвращает правильное следующее состояние
2. **Данные в context:** `user_data` заполняется корректно
3. **Google Sheets:** Новая запись добавлена
4. **Расчеты:** Все поля рассчитаны правильно

---

## 📝 Важные замечания

### 1. Динамическая ставка (Dynamic Rate)

⚠️ **Тест учитывает кумулятивные продажи!**

Если пользователь уже создавал смены сегодня, динамическая ставка будет рассчитываться на основе **суммарных продаж за день**.

**Пример:**
```
Shift 1: $500 → 10% (8% base + 2% dynamic)
Shift 2: $500 → 12% (8% base + 4% dynamic) ← сумма за день $1000!
```

Тест автоматически определяет фактическую ставку и использует ее для проверок.

### 2. Таймзона

Все даты/время в **America/New_York** (UTC-5 + DST).

### 3. Зависимости

Тест требует:
- Доступ к Google Sheets API
- Файл `google_sheets_credentials.json`
- Файл `.env` с настройками
- Установленные зависимости (`requirements.txt`)

---

## 🚀 Создание новых тестов

Используйте `test_scenario_2_1.py` как шаблон для других сценариев из `TEST_SCENARIOS.md`.

### Структура нового теста:

```python
class BotTestSimulator:
    async def run_test(self):
        # 1. Инициализация
        context = self._create_mock_context()

        # 2. Шаги теста
        update = self._create_mock_update(callback_data="...")
        result = await handle_callback_query(update, context)
        assert result == EXPECTED_STATE

        # 3. Проверка результатов
        verify("Field name", expected, actual)
```

### Примеры callback_data:

```python
"CREATE_SHIFT"         # Создать смену
"EDIT_SHIFT"           # Редактировать смену
"DATE_IN:0"            # Server date
"DATE_IN:-1"           # Server date - 1
"TIME:IN:09_AM"        # 9 AM (Clock in)
"TIME:OUT:05_PM"       # 5 PM (Clock out)
"PROD:Model A"         # Выбор продукта
"ADD_MODEL"            # Добавить продукт
"FINISH"               # Завершить смену
"BACK"                 # Назад
```

---

## 🐛 Отладка

### Включить подробные логи:

```python
logging.basicConfig(level=logging.DEBUG)
```

### Посмотреть созданные смены:

```bash
python3 check_today_shifts.py
```

### Проверить Google Sheets напрямую:

```bash
python3 tests/utils/check_today_shifts.py
```

Или через Python:
```python
from sheets_service import SheetsService

sheets = SheetsService()
shifts = sheets.get_all_shifts()
print(shifts[-1])  # Последняя смена
```

---

## 📊 Метрики тестирования

| Сценарий | Статус | Автоматизирован |
|----------|--------|-----------------|
| 2.1: Создание смены (1 продукт) | ✅ PASS | ✅ Да |
| 2.2: Создание смены (несколько продуктов) | ✅ PASS | ✅ Да |
| 2.3: Дубликат продукта | ⬜ TODO | ❌ Нет |
| 2.4: Валидация времени | ⬜ TODO | ❌ Нет |
| 3.1: Редактирование Clock in | ⬜ TODO | ❌ Нет |
| ... | ... | ... |

**См. полную документацию:** [tests/README.md](tests/README.md)

---

## ✅ Преимущества автоматизации

1. **Быстро:** Тест выполняется за ~5 секунд
2. **Надежно:** Одинаковые результаты при каждом запуске
3. **Полно:** Проверяет ВСЕ аспекты (от UI до расчетов)
4. **Документация:** Тест = живая документация кода

---

**Автор:** Claude AI
**Дата:** 2025-11-01
**Версия:** 1.0
